---
type: log
title: "filament-core-data specification update log"
description: "Chronological log of structural changes to the requirements bundle."
---
# filament-core-data specification update log

## History

* **2026-09-17** — Issue #154: the extraction frontend pins `quire-rs` `96df8b1` and module `spec-objects-business@0.4.0`, and lifts states, transitions, steps and vocabulary into the IR. FR-143 gains AC-6 (TC-1785: an engine declaration the construct cannot lower refuses the artifact, including relationship rows the engine did not extract); TC-1755 is un-blocked. FR-091, FR-095 (state/transition/step identity slots), FR-098 (module revision `f7fdfda`), FR-086 and NFR-033 (workspace channel `1.98.1`, which the pinned `quire-rs` requires) are amended.

* **2026-09-16** — Issue #93 (presence half): FR-106 states the semantic IR presence rule. `Field.presence` is authored as `required` or `optional` and carried independently of `multiplicity`, `nullable` and the default kind; `PRESENCE_MULTIPLICITY_MISMATCH` applies to `1.1.0` documents only.

* **2026-08-29** — Initialized the semantic data architecture requirements bundle for issue #8.
* **2026-08-29** — Added the read-only Filament contract-census requirements for issue #10.
* **2026-08-29** — Added the pinned, isolated TypeSpec feasibility and structural-schema decision requirements for issue #4.
* **2026-08-30** — Added the issue #9 semantic IR, package, mapping, representation, generated-target, compatibility, and non-disruption requirements.
* **2026-09-03** — Issue #30: recorded the owner decision (TypeSpec is the structural source, ADR-0005). Removed the JSON Schema fallback and the "owns and funds the compensation" pass condition from FR-018 and US-004; retitled FR-018.
* **2026-09-03** — Issue #9: FR-019 and US-005 rewritten from the JSON Schema fallback to TypeSpec as the v1 structural source per ADR-0005; TC-199 resolved by the owner decision on issue #4. The 13 contract schemas are unchanged.
* **2026-09-03** — Issue #30 (follow-up): removed the remaining JSON Schema fallback mandate from FR-007 (behavior, AC-2), FR-008-AC-4, and US-002 (EX-1, AC-1); TC-026/TC-032/TC-036 retitled.
* **2026-09-03** — Issue #34 (Task-035): FR-030 manifest-target binding widened from the five-value generated-target enumeration to the declared registry (generated targets ∪ representation formats) because the frozen v1 manifest fixture selects `markdown`; TC-229/TC-230 retitled.
* **2026-09-03** — Issue #34: added US-006, FR-027 (multiplicity and units), FR-028 (relationships, operations, clauses), FR-029 (closed constraint vocabulary), FR-030 (source dialect and manifest target binding), NFR-013 (additive IR revision); amended FR-020 Behavior and AC-7/AC-8; spec.md scope updated for IR v1.1.
* **2026-09-03** — Issue #35: added US-007, FR-031 (declaration grammar in TypeSpec), FR-032 (kernel scalar library), FR-033 (JSON Schema projection with the #31 normalization pinned), FR-034 (lowering to IR v1.1 with zero loss; UCUM `UnitSymbol` carries SR-036 FND-113), NFR-014 (small kernel discipline); spec.md scope updated.
* **2026-09-03** — Issue #20: added US-008, FR-035 (conformance corpus, versioning, minimization, provenance), FR-036 (independent JSON-level semantic oracle), FR-037 (differential harness, adapter registry, divergence register), FR-038 (construct register with four case classes per row), FR-039 (coverage accounting, promotion thresholds, import API), NFR-015 (blessing-free deterministic evidence), NFR-016 (isolated corpus); spec.md scope updated. Ids were allocated against `main` at 8425a14; issue #27 had allocated none.
* **2026-09-03** — Issue #20 (review pass): SR-047..054 findings applied. The case input became an input bundle composing the published package, lock, mapping, profile, and consumer-policy schemas; expected diagnostics became `common.schema.json#/$defs/diagnostic` documents with the pointer as corpus metadata; a diagnostic-code register was added; the alias cycle and the depth bound were separated; `resultState` was made exhaustive and disjoint; the compatibility classification was scoped to the IR surface with a declared restrictiveness order; the harness lost its clock; the divergence register gained an owner and a verdict and a sibling contract-gap register; the `./conformance` package export was dropped to issue #11; and the construct register grew to 22 families with a justified `notApplicable` escape. TC rows became TC-280..341.
* **2026-09-03** — Issue #27: added US-009, FR-040 (prototype disposition inventory), FR-041 (promoted semantic-IR emitter and narrow build interface), FR-042 (TypeScript and Rust backends), FR-043 (governed Python JSON Schema adapter), FR-044 (frozen-spike replay with one declared evidence delta), NFR-017 (deterministic promoted compilation, including the retained Rust lockfile seeding fix), NFR-018 (non-disruptive promotion and clean rollback); spec.md scope updated. Ids skip US-008, FR-035..039 and NFR-015..016, which the parallel issue #20 conformance-corpus branch allocated first.
* **2026-09-04** — Issue #19: added US-010, FR-045 (frontend seam and dialect registry), FR-046 (TypeSpec lowering to contract IR 1.1.0 with the semantic decorator library), FR-047 (package graph resolution with exact JSON loci), FR-048 (lock, canonicalization, and v1 fingerprint), FR-049 (closed diagnostic registry, ordering, and limits), FR-050 (IR validation, normalization, and fingerprint), FR-051 (compatibility diff and IR schema-evolution projections), FR-052 (`compile`, `inspect`, and `diff` commands); NFR-019 (deterministic contract compilation), NFR-020 (bounded and safe compilation of untrusted inputs), NFR-021 (non-disruptive compiler core); spec.md scope updated. FR-046-CON-1 declares that the frozen FR-041 prototype IR is retained unchanged and the contract IR is a second lowering beside it, so no issue #4 golden is regenerated. Ids skip US-008, FR-035..039 and NFR-015..016, which the parallel issue #20 branch allocated first.
* **2026-09-04** — Issue #19 (follow-up): NFR-021 re-scoped from "the whole branch" to this change's own path set, fixed at both ends by history, after issue #20 measured the gate annexing a later ticket's paths and failing issue #19 for them; added NFR-021-AC-10 (a later change adds no path to this change's set) and TC-644. No permitted or prohibited path changed.
* **2026-09-04** — Issue #22: added US-012, FR-063 (generation backend seam and the TypeScript target contract), FR-064 (lower IR type definitions to TypeScript), FR-065 (ESM package and export surface), FR-066 (generated runtime validators), FR-067 (identity and fingerprint metadata), FR-068 (IR admissibility and declared loss), FR-069 (canonical form and IR-surface classification), FR-070 (the `typescript-backend` conformance adapter), FR-071 (the `generate` command and the surface fixtures); NFR-024 (portable, deterministic generated TypeScript), NFR-025 (non-disruptive TypeScript backend); spec.md scope updated. Ids were allocated as an exclusive range against `main` at c1b8807 because issues #21 and #23 are authored in parallel: US-012, FR-063..071, NFR-024..025, TC-745..844, SR-087..096, FND-1000..1099. GAP-011 is recorded as an open dependency on issue #9 and is not decided here.
* **2026-09-04** — Issue #22 (review pass): SR-087..SR-094 findings applied. The resolved type model gained an owner and a declared shape (FR-064 `buildModel`); the emitted file set grew to eight with a generated `errors.ts`; the `as` prohibition became a syntactic ban on type assertions with `as const` exempt; every bundler-dependent obligation became a static reachable-symbol walk, because no bundler is resolvable in this repository and both lockfiles are frozen; generated text is rendered through an injected formatter backed by the pinned `@biomejs/biome` binary, which is how the issue's deterministic-formatted-output deliverable is discharged; the generated fixture typechecks in its own program because the root `tsconfig.json` lacks `exactOptionalPropertyTypes` and sets `noUnusedLocals`; every claim that a corpus case supplies an instance payload was removed — the corpus carries none — and replaced by an authored instance corpus cross-checked against `ajv@8.20.0`; every whole-corpus absolute became this slot's own delta, because issues #21 and #23 regenerate the same accounts; FR-068 gained a suppression channel, a declared severity and locus reading, a per-code derivation ledger and a first-run divergence count; FR-069 gained the field and variant classification rules the published policy requires and the normative version-uplift round-trip rule; FR-070 gained the agreement obligation it lacked and a prohibition on registering any divergence; and NFR-025's three merge-base comparisons became history-pinned or tree assertions. Four gaps were filed rather than decided here: #61 (a diagnostic's severity and locus are derivable from no published artifact), #62 (the compiler's depth limit 128 against the corpus's 256), #63 (three concurrent backend tickets regenerate the same four shared machine-generated artifacts), and #64 (the normative version-uplift rule against the oracle's flat classification). `agent-ix/filament-core-data#9` is closed, so every citation naming it as a live decider now cites the gap row and #59.
* **2026-09-04** — Issue #23: added US-013, FR-072 (pinned toolchain and advisory floor), FR-073 (immutable per-family target profiles), FR-074 (owned schema-preparation pass, additive to FR-043), FR-075 (schema and option refusal guards over the five executable keys the pinned 0.76.0 actually binds), FR-076 (sandboxed deterministic runner outside `src/compiler/` so FR-043-AC-8 stays green), FR-077 (per-family qualification verdicts, probe corpus, and retained-gap register), FR-078 (generated-source inspection for silent `Any` and unconstrained-mapping degradation), FR-079 (package layout, provenance, and examples), FR-080 (strict static checking and falsifiable runtime validation); NFR-026 (sandboxed generation from untrusted schema), NFR-027 (reproducible and non-disruptive generation). Ids skip FR-054..FR-071 and NFR-022..NFR-025, which the parallel issue #21 and #22 backend branches allocated first. No published schema, fixture, corpus file, or issue #4 golden changes; nothing is published.
* **2026-09-04** — Issue #23 (review pass): SR-097..SR-104 findings applied. The adapter moved from a Node/Python split to one Python surface, because a Python runner cannot call a JavaScript guard before it spawns anything (FND-1101, FND-1156, FND-1166). `--extra-fields forbid` was removed from every profile: measured against the pinned generator, `additionalProperties: false` already closes both Pydantic families and `TypedDict`, and the blanket flag closes models the schema leaves open (FND-1115). `--field-constraints`, `--use-annotated`, and `--strict-nullable` were added because the `conint`/`constr` call form is not a valid annotation and fails strict type checking, and because a defaulted non-nullable field otherwise renders `T | None`. `--formatters builtin` was fixed as the value, removing black and isort from generation entirely (FND-1158). Host-observed versions left every byte-compared artefact and the toolchain fingerprint, which now covers only the declared pins, so the issue #42 coupling is not reproduced (FND-1127, FND-1137, FND-1157). FR-074 no longer runs FR-043's normalizer over published schemas, which would destroy the identities their cross-file `$ref`s resolve through (FND-1116), and the multi-document input mode is now stated (FND-1123, FND-1160). FR-078 gained a reporting mode so a lossy family can be measured rather than suppressed (FND-1111, FND-1139, FND-1178) and a stated attribution rule (FND-1122). The verdict vocabulary was closed to three values with `demonstrated profile` defined (FND-1112, FND-1113). FR-076's limits became stated values in `limits.json` (FND-1119, FND-1130). The corpus obligation was re-scoped: the `python-backend` slot's owning issue is #23, not #52, and it needs an IR reader a generated type package cannot be, so this change delivers a read-only advisory account and files the reader (FND-1133..FND-1136, FND-1168). NFR-027 took on converting the six suites of issue #51 rather than widening six permitted-path lists (FND-1155, FND-1170, FND-1171). ERR rows moved to ERR-132..143 after colliding with twelve merged rows (FND-1100, FND-1132). TC rows were re-mapped to the corrected criteria and retyped per SR-101 (FND-1144..FND-1150).
* **2026-09-04** — Issue #22 (merge integration): FR-069's variant-addition rule was self-contradictory — its Behavior said `conditional` where a policy admits unknown members and `breaking` otherwise, while FR-069-AC-17 said `conditional` with no policy at all, and neither matched `docs/semantic-data-system/compatibility.md`, the authority both cite. The requirement now states that document's rule — `additive` for an open-enum consumer, `breaking` for a closed one — and carries `VARIANT_ADDITION_POLICY`, a single named constant with two settings in the manner of `VERSION_UPLIFT_POLICY` and `REFERENCE_POLICY`, defaulting to the corpus's published `conditional` reading. Both settings were measured: 25 of 25 compatibility cases agree under `corpus`, 23 of 25 under `contract`, differing on exactly `ENUM-004` and `UNION-004`. The corpus is not moved here because FR-070 forbids resolving a disagreement by editing a corpus case and NFR-025 makes `conformance/cases/**`, `conformance/corpus.json`, `conformance/contract-gaps.json` and `conformance/divergences.json` prohibited paths; the disagreement is reported to the corpus's owner instead. FR-076-AC-12 and FR-076-CON-1 gained the named exemption for FR-071's injected formatter, which is the one module under `src/compiler/` that starts a child process by design and which no backend can reach — the Python gate TC-894 now carries the same named exemption and the same reachability assertion the JS gate already carried.
* **2026-09-04** — Issue #21: added US-011 (Rust consumer outcome), FR-054 (IR-to-Rust/Serde mapping table), FR-055 (stable, collision-free identifier derivation), FR-056 (crate emission, static export surface, provenance, output manifest), FR-057 (constraint enforcement and the ECMA-262-under-RE2 decision procedure, which resolves GAP-002 with a proved hand-written `sourceLocus.path` validator rather than a weakened pattern), FR-058 (closed `agent-ix.rust-backend.*` diagnostic registry and refusal of unsupported or lossy constructs), FR-059 (the `rust-backend` conformance adapter and its independent Rust reader), FR-060 (determinism, rustfmt-cleanliness, and the MSRV/support matrix), FR-061 (compile-time and runtime consumers built from the packaged artifact), FR-062 (mapping-branch register, property tests, and mutation catalogue); NFR-022 (deterministic and hermetic Rust generation), NFR-023 (non-disruptive Rust backend, with the changed-path range fixed at both ends by history per the PR #54 form). FR-059 records a dependency on GAP-011 and does not decide it. Ids were allocated against `main` at c1b8807; the parallel issue #22 and #23 backend branches allocate from their own reserved ranges.
* **2026-09-04** — Issue #21 (review pass): SR-077..084 findings applied — 91 findings, 35 high, 47 medium, 9 low. The three-axis field composition gained the serde attributes that make an absent member and a present `null` distinguishable (`default`, `skip_serializing_if`, and a `present_or_absent` deserializer); the mapping table is keyed on `kind` alone; the recursion graph covers every reference edge, not only fields; identifiers derive from the semantic identity rather than from `displayName`, and refuse rather than mangle a character they cannot render; the pattern machinery declares its ECMA-262 dialect (no flags, UTF-16 code units) and its supported subset; ISO 8601 bounds compare instants and `duration` bounds are refused as unordered; `SemanticValue` retains number lexemes and object member order so a round trip is a fact about the value; `bytes` is refused because no artifact declares its wire form; seven diagnostic codes were added so every previously undisposed construct has a named refusal; the Rust workspace, the third-party attribution register and the offline supply gained owning requirements; FR-059 registers no divergence and records GAP-002's answer and GAP-011's dependency in `docs/semantic-data-system/rust-backend.md`; and NFR-023's changed-path range gained a `--first-parent --no-merges` union, named sentinels, and a checkable permitted-list criterion. Five findings are fixed only as far as this branch may go and are filed as issues #56, #57, #58, #59 and #60. The criterion set grew from 155 obligations to 204 over the same TC block.
* **2026-09-04** — Issue #21 (implementation): six requirement corrections, each forced by measured evidence rather than by convenience, and each recorded in the commit that made it. FR-054's `unknownPolicy` refusal on a non-record kind was wrong about the contract — the independent oracle decides `core-1-1` and `package-1-1`, which carry a `preserve` map and a `surface` union, as `success` — so the policy is now mapped on all eight kinds and `UNKNOWN_POLICY_ON_NON_RECORD` left the registry. FR-056-AC-12's "no filesystem call" was unreachable because the pinned tables are read at module load, and now asks for no *ambient* input with the committed tables named. FR-054-AC-15's byte-identity claim for a number written `1.0` was unreachable with `serde` alone, whose data model never hands a visitor the source lexeme, and is now claimed only for members the crate retains bytes for. FR-058 gained a second namespace: five leaf names were declared under both `agent-ix.rust-backend.*` here and `agent-ix.semantic-ir.*` in the published set, which is SR-066 FND-500 one layer down, so each code now lives in the namespace that owns its defect, and `UNKNOWN_REQUIRED_EXTENSION` joined it when FR-059-AC-10's five published cross-language verdicts turned out to be decided by nothing. FR-060 and NFR-023 gained `scripts/build-rust-backend-goldens.mjs`, which existed and was owned by no requirement — the hole NFR-023-AC-11 was rewritten to catch, and did. FR-059's `conformance/` write set grew from one path to two: the generated `coverage.json` necessarily moves when the slot it accounts for is filled, and the permission is bounded by a gate asserting no other adapter row, no unmet area and no other member can travel through it. Filed against their artifacts' owners because each touches a prohibited path: #56, #57, #58, #59, #60, #69.
* **2026-09-04** — Issue #23: added US-013, FR-072 (pinned toolchain and advisory floor), FR-073 (immutable per-family target profiles), FR-074 (owned schema-preparation pass, additive to FR-043), FR-075 (schema and option refusal guards over the five executable keys the pinned 0.76.0 actually binds), FR-076 (sandboxed deterministic runner outside `src/compiler/` so FR-043-AC-8 stays green), FR-077 (per-family qualification verdicts, probe corpus, and retained-gap register), FR-078 (generated-source inspection for silent `Any` and unconstrained-mapping degradation), FR-079 (package layout, provenance, and examples), FR-080 (strict static checking and falsifiable runtime validation); NFR-026 (sandboxed generation from untrusted schema), NFR-027 (reproducible and non-disruptive generation). Ids skip FR-054..FR-071 and NFR-022..NFR-025, which the parallel issue #21 and #22 backend branches allocated first. No published schema, fixture, corpus file, or issue #4 golden changes; nothing is published.
* **2026-09-04** — Issue #23 (review pass): SR-097..SR-104 findings applied. The adapter moved from a Node/Python split to one Python surface, because a Python runner cannot call a JavaScript guard before it spawns anything (FND-1101, FND-1156, FND-1166). `--extra-fields forbid` was removed from every profile: measured against the pinned generator, `additionalProperties: false` already closes both Pydantic families and `TypedDict`, and the blanket flag closes models the schema leaves open (FND-1115). `--field-constraints`, `--use-annotated`, and `--strict-nullable` were added because the `conint`/`constr` call form is not a valid annotation and fails strict type checking, and because a defaulted non-nullable field otherwise renders `T | None`. `--formatters builtin` was fixed as the value, removing black and isort from generation entirely (FND-1158). Host-observed versions left every byte-compared artefact and the toolchain fingerprint, which now covers only the declared pins, so the issue #42 coupling is not reproduced (FND-1127, FND-1137, FND-1157). FR-074 no longer runs FR-043's normalizer over published schemas, which would destroy the identities their cross-file `$ref`s resolve through (FND-1116), and the multi-document input mode is now stated (FND-1123, FND-1160). FR-078 gained a reporting mode so a lossy family can be measured rather than suppressed (FND-1111, FND-1139, FND-1178) and a stated attribution rule (FND-1122). The verdict vocabulary was closed to three values with `demonstrated profile` defined (FND-1112, FND-1113). FR-076's limits became stated values in `limits.json` (FND-1119, FND-1130). The corpus obligation was re-scoped: the `python-backend` slot's owning issue is #23, not #52, and it needs an IR reader a generated type package cannot be, so this change delivers a read-only advisory account and files the reader (FND-1133..FND-1136, FND-1168). NFR-027 took on converting the six suites of issue #51 rather than widening six permitted-path lists (FND-1155, FND-1170, FND-1171). ERR rows moved to ERR-132..143 after colliding with twelve merged rows (FND-1100, FND-1132). TC rows were re-mapped to the corrected criteria and retyped per SR-101 (FND-1144..FND-1150).
* **2026-09-04** — Issue #21 (gate repair): the process-spawning half of the Rust backend's command line moved out of `src/compiler/`, which FR-042-AC-4 holds to spawning nothing. FR-056 now splits its command line across `cli.mjs` (the pure verbs) and `scripts/rust-backend-harness.mjs` (the verbs that need a child process); FR-057's differential harness path follows the move to `scripts/`; FR-062 and FR-058 declare the nine modules under `src/compiler/backends/rust-serde/` that no ledger owned, which FR-040-AC-5's exactly-once census found. Added FR-060-AC-16 and TC-945: the support matrix records every lint the generated `[lints.rust]` table denies and the toolchain coupling `warnings = "deny"` creates, and the gate reads that lint set out of a generated `Cargo.toml` rather than restating it. NFR-023's Verification names how a byte-freeze gate resolves its *head* endpoint, after the post-merge rehearsal with an unrelated sibling on top failed issue #21 for a later ticket's edit to `src/compiler/backends/typescript.mjs` — NFR-023-AC-9 and AC-10 already forbade it. No permitted or prohibited path changed, and nothing was published.
* **2026-09-04** — Issue #11: added US-014, FR-081 (semantic kernel bundle declaration and generation manifest), FR-082 (JSON Schema 2020-12 lowering to one semantic IR v1.1 kernel document, behind a new source form on the frontend seam), FR-083 (deterministic minting of names and identities for anonymous schema constructs), FR-084 (kernel provenance and the closed representability-loss register), FR-085 (kernel TypeScript package), FR-086 (kernel Rust crate), FR-087 (kernel Python package and the pure reference-localization pass), FR-088 (modular kernel JSON Schema as a package artifact), FR-089 (independent per-language consumer examples), FR-090 (cross-language agreement and the publication gate); NFR-028 (deterministic and hermetic kernel generation), NFR-029 (portable, dependency-free kernel packages), NFR-030 (non-disruptive kernel packaging behind `agent-ix/quoin#290`). Ids were reserved as an exclusive range rather than scanned for a maximum, after issues #21 and #22 both scanned, both found `ERR-113`, and both allocated `ERR-114..131`: US-014, FR-081..090, NFR-028..030, TC-1000..1120, ERR-200..240, EC-100..130, SR-140..150, FND-1300..1399.
  The kernel's own TypeSpec source cannot reach the issue #19 frontend — measured: it rejects `TypeRef.target`'s union-typed property, `MinConstraint.keyword`'s enum-member type, `PatternConstraint.dialect`'s string-literal type, `DefaultDecl.value`'s unconstrained type, and `ConstraintDecl`'s unnamed union variants — so the lowering reads the official emitter's JSON Schema projection, which `make semantic-core-check` already freezes byte for byte. That projection is a deterministic transport of `main.tsp`, so the IR stamps `source.dialect: "typespec"`; `common.schema.json#/$defs/frontendDialect` is closed at two members and is not widened here, and the contract owner is asked whether a projection deserves its own value.
  Two representability losses are declared rather than repaired: IR v1.1 has no any-type, so `DefaultDecl.value` narrows to the `JsonObject` lowering `packages/semantic-core/kernel-scalars.json` already prescribes; and `presence` is derived from `multiplicity.lower`, so a required-but-possibly-empty collection such as `OperationDecl.params` cannot be stated — adjacent to SR-081 FND-956. Each carries a diagnostic code, a register row, and an issue against the contract owner.
  Nothing is published. `package.json` is a prohibited path, the generated Rust manifest carries `publish = false` unconditionally, and no generated manifest names a registry.
* **2026-09-08** — Issue #36: added US-015, FR-091 (read a spec bundle through the Quire extraction contract in-process), FR-092 (closed type-token resolver with explicit failure states), FR-093 (lower field declarations to IR record fields with the declared #78 losses), FR-094 (relationships from FR-040 edges, operations, opaque clauses), FR-095 (package identity, source and package blocks, provenance; `spec-bundle` dialect as the declared reading of #77), FR-096 (closed `agent-ix.extraction-frontend.*` registry; engine loci carried through as the declared reading of #61), FR-097 (in-crate schema validation, unextended RFC 8785 serialization with identity-sorted node lists as the declared reading of #67, fingerprint, atomic write), FR-098 (provenance-tracked fixtures, goldens, negatives, shared-case parity with the TypeSpec frontend; the live `config-service` FR-006 is legacy-form and is a negative control), FR-099 (`extraction-frontend` command line and Make targets); NFR-031 (deterministic and hermetic lifting), NFR-032 (non-disruptive extraction frontend with a history-pinned changed-path range), NFR-033 (exact Rust 1.98.1 for the crate only, pinned and licensed dependencies). Ids were reserved as an exclusive range against `main` at 3b75e01 and every remote branch, because `contract-agent-core/49-scratch-fixtures` (PR #84) is open in parallel: US-015, FR-091..099, NFR-031..033, TC-1200..1349 (NFR rows at TC-1300..1329; TC-1330..1349 allocated by the matrix for shared-constraint, blocked, and review-added rows), SR-160..170, FND-1400..1499, ERR-250..280, EC-140..160, Plan-014. Phase 0 feasibility (2026-09-08 comment on #36): the `json-schema` target has no IR-reading backend (filed #85; #36 AC-5 carried as a declared gap), no quire-rs release tag contains #388 (pinned by `rev`), the installed `spec-objects-business` module is 0.2.0 without a `semantic` block while `main` is 0.3.0 (modules load from explicit paths only). No byte of `schema/**`, `src/compiler/**`, `packages/**`, or `crates/semantic-ir/**` changes; nothing is published.
* **2026-09-08** — Issue #36 (review pass): composite review SR-160..SR-168 under `spec/reviews/36-extraction-frontend/`, review set `all` inherited as the repository's standing choice. 13 high and 33 medium findings applied: `crates/semantic-ir` becomes a runtime path dependency so `decide` runs at lift time and the canonical bytes are its `normalized` form (no third canonicalizer, no `jsonschema` crate); the diagnostic namespace is closed to `agent-ix.extraction-frontend.*` (26 codes); FR-092 classifies the engine's resolution rather than re-resolving (no `Ambiguous`; imports refused as `IMPORT_UNSUPPORTED`); enumerations lower to `enum` through the engine's extraction DSL; relationships come from frontmatter edges filtered by `allowed_links`, with `composite` read from the registry's `inverse: part_of`, pending `agent-ix/quire-rs#418`; cross-frontend parity is structural on a new shared case pending the seam wiring in #86; quire-rs is pinned at a revision at or after `a874fb6`; the business module is vendored from `d1840b8`. Residues FND-1481..1484 applied; FND-1480 stands as an ordering rule: criteria that read the FR-050 reader (FR-092-AC-10, FR-093-AC-11, FR-094-AC-15, FR-097-AC-11) are tasked after PR #84 lands and this branch rebases onto it. Matrix: 150 rows TC-1200..1349, 174/174 obligations traced.

* **2026-09-08** — Issue #36 (CR-036-1): Task-127 found that `cargo` enforces every member's `rust-version` under `--workspace`, so the crate's member-level `rust-version = "1.98.1"` made `make rust-build` and `make rust-test` on the `rust-toolchain.toml` channel `1.94.1` refuse the whole workspace ("rustc 1.94.1 is not supported by agent-ix-extraction-frontend") and broke `make test` for every other member — a NFR-032 non-disruption violation, while the graph compiles on 1.94.1. Decision: the supported minimum and the qualification compiler are named separately, as `agent-ix/quire-contract-ir` PR #62 did — the manifest declares `rust-version.workspace = true` (1.85.0, like the sibling members) and exact 1.98.1 stays the qualification toolchain named once in the `Makefile` (`EXTRACTION_TOOLCHAIN ?= 1.98.1`), every `extraction-frontend-*` gate running `cargo +$(EXTRACTION_TOOLCHAIN)`. NFR-033: Statement, Rationale, metrics, Verification, AC-1 (manifest asserts `rust-version.workspace = true` and the single Makefile line; TC-1320 kept), AC-2 (`+$(EXTRACTION_TOOLCHAIN)`), new AC-11 (`cargo check -p agent-ix-extraction-frontend --locked --offline` on the workspace channel exits zero; TC-1350, the next id after the block, checked free on every remote branch). Three corrections landed with it: the metric "`jsonschema` crates reachable: 0" becomes "`jsonschema` declared as a direct dependency: 0" (quire-rs links it transitively by design, and it is in the notices file); the clippy gate is `--no-deps` in NFR-033-AC-7, the metric, and FR-099's `extraction-frontend-test` (the sibling members fail 1.98.1's newer lints and are not qualified on it); `cargo audit` has no `--locked` flag, so FR-099's `extraction-frontend-audit` drops it. Matrix: TC-1320, TC-1321, TC-1326 titles rewritten, TC-1350 added, NFR-033 summary row updated. The workspace `rust-version` and `rust-toolchain.toml` remain untouched.
* **2026-09-08** — Issue #36 (CR-036-2): Task-128 found three unrealisable criteria. (1) `common.schema.json#/$defs/diagnostic` types `causes.items` as `diagnostic`, whose `code` pattern admits only `agent-ix.*`, so an `ENGINE_DIAGNOSTIC` carrying the engine's `semantic.*` code in `causes[0]` (FR-091 behaviour, FR-091-AC-9, FR-096 Rationale and behaviour, FR-096-AC-4) could never satisfy the schema-validity criteria FR-091-AC-11, FR-096-AC-1, and FR-096-AC-15. Decision: the engine code and reason open the message as `<engine code> (reason: <reason>): <engine message>`, `causes` is `[]`, and the document stays schema-valid; TC-1208, TC-1262, and ERR-274 restated. (2) Under quire 0.31.0 the `rust-test-name-id` form binds through the `tc_NNNN_` name as well as the `#[trace]` marker, so NFR-033-AC-9 now removes both the marker and the name prefix to produce the status lie; TC-1328 restated. (3) FR-091-AC-4's conflicting module cannot drop `versionNumber` through a `semantic` block (extraction reads only `semantic_core`, `package`, `compatibility_posture`, `legacy_forms`); the control now differs by `compatibility_posture: declared-lossy` and `legacy_forms: error`; TC-1203 kept. No crate or plan file changes.
* **2026-09-08** — Issue #36 (CR-036-3): corrections found by implementation Tasks 129..133, each with the orchestrator's decision, applied spec-only. FR-091: `MODULE_REFUSED` opens its message with the engine `semantic.*` code with `causes` empty, the same reading as `ENGINE_DIAGNOSTIC` after CR-036-2 (behaviour, AC-3); CON-2 admits one `std::fs::read` site in `bundle.rs` for exactly `<module root>/manifest.yaml`, since quire-rs exposes no manifest-bytes API. FR-092: by-id resolution applies only to identifier-shaped ids — a `Type` cell `FR-005` is rejected upstream by quire-rs FR-070 `is_identifier` as `semantic.invalid-type-token` (AC-2, AC-6, Rationale); DECISION on kernel-name collision: an artifact whose `displayName` equals a kernel scalar the bundle *uses* is blocking `DUPLICATE_TYPE_NAME` at that artifact, `KERNEL_NAME_SHADOWED` (warning) applies only when the bundle does not use the scalar (behaviour, AC-8; the lift-level outcome stated in FR-093-AC-13 and FR-095-AC-14, TC-1347). FR-093: table/fence identity holds for `types[]` and diagnostics with `source.digest` differing by construction, field origin column is where the declaration text begins (table: name-cell start; fence: first non-blank column), fixtures line-aligned (AC-1, CON-1, field origin rule); `appliesTo` is the field identity and the `crates/semantic-ir` reader checks applicability only for type-scoped subjects, so the frontend's gate is its own and CON-4's contract test compares the two tables over type-scoped subjects, FR-034's alias-per-constrained-field form not used (AC-6, CON-4); no "module marks required" marker exists — one `DECLARED_LOSS` `required-collection-presence` per `0..*`/`*` collection field (AC-8); `<KEYWORD>` is SCREAMING_SNAKE (`MAX_LENGTH`); module short name = manifest `name` minus the `spec-objects-` prefix; `ext/identity-field`/`ext/decimal-policy` (`required: false`) recorded as distinct by design from FR-034's `ext/identity`/`ext/decimal` (`required: true`); Rationale cites agent-ix/quire-rs#420 (`SemanticExtraction.fields` carries no per-field locus; rows re-derived through the engine's scan/`table_rows`). FR-094: spec-objects-business `d1840b8` declares no `edge_types`, the FR-040 registry is vendored from spec-artifacts-iso as `fixtures/modules/edge-vocabulary` (manifest lines 872–975) and every lift loads both module roots (Inputs, AC-2, AC-3); AC-8 restated — `parent` is a field, no parent relationship, the remaining node agrees with the #34 fixture on `target` and `multiplicity`, the #34 fixture sha pinned (its `belongs_to` came from the removed `## Relationships` grammar and its identity patterns differ); `lower_clauses` returns `Result<_, LowerError>` (`UNSLUGGABLE_NAME` on a `clauseId`); `allowed_links` are resolved for the object archetype only, never the artifact axis; param-row constraints are not lowered because an `operation.params[]` item is a schema `field`, which has no `constraints` member (verified against `semantic-ir.schema.json`). FR-095: `<module package>` in `lockDigest` is the module's `semantic.package` (AC-4); slug alphanumerics are ASCII only, non-ASCII letters are separators; `manifestDigest` is over every loaded module in module-name order (AC-4, AC-11). FR-098: `modules/edge-vocabulary/` added to the fixture inventory with its provenance; FR-099: AC-1 and `extraction-frontend-lift` name both module roots. NFR-031: AC-3 and the cross-form metric restated for `types[]`/diagnostics; AC-5 exemption list and the files-opened metric admit the `bundle.rs` manifest read. Matrix: TC-1202, TC-1211, TC-1215, TC-1217, TC-1220, TC-1225, TC-1227, TC-1232, TC-1238, TC-1249, TC-1256, TC-1302, TC-1347 titles rewritten; no row added or removed, every TC id stays on its AC. No crate or plan file changes.
* **2026-09-09** — Issue #36 (CR-036-4): Task-134 left TC-1283, TC-1219, and TC-1230 `#[ignore]`d because the FR-050 node reader (`src/compiler/ir/reader.mjs`) resolves `constraint.appliesTo` as a type identity and raises `UNRESOLVED_TYPE_REF` over every field-scoped constraint FR-093 emitted, while `agent_ix_semantic_ir::decide` accepts either. Decision: adopt the repository's established form, FR-034's alias-per-constrained-field (`VersionNumber` in `fixtures/semantic/v1/positive/config-version-v1-1.json`) — one `kind: alias` at `type/<DisplayName>.<fieldName>` per field carrying constraints, `target` the resolved type, `constraints[]` with `appliesTo` the alias identity, the field's `typeRef` the alias; an unconstrained field is unchanged and a record's own `constraints[]` is `[]`. FR-093: Description, Outputs, "The fields" (three new bullets and the constraint bullet; the `CONSTRAINT_NOT_APPLICABLE` bullet no longer claims the reader checks only type-scoped subjects), the enumeration "no alias" sentence, Rationale, CON-2, CON-4, AC-4, AC-5, AC-6, AC-10, AC-12. FR-095: the alias pattern joins the closed identity list. FR-097: the canonical-form node lists also name field/param `extensions`, `occurrences`, and top-level `extensions` (FR-050 `IDENTITY_SETS`); AC-1 asks for no direct `jsonschema` edge (quire-rs links it transitively, NFR-033); AC-4's reference is the code-point sort under two `LC_ALL` values, not `Intl.Collator`, whose case-insensitive primary level orders `Ordering` before `OrderLifecycle`. FR-098-AC-3 names `alias`. Matrix: TC-1223, TC-1224, TC-1225, TC-1229, TC-1273, TC-1276, TC-1287, TC-1333 titles rewritten; no row added or removed, every TC id stays on its AC. Crate: `lower.rs` mints the alias (`identity.rs` `alias_identity`); TC-1283, TC-1219, TC-1230 un-ignored and green. Operation parameters are unchanged: FR-094 keeps its CR-036-3 rule, since no fixture declares a parameter constraint and FR-095 mints no parameter-alias or parameter-constraint pattern.
* **2026-09-09** — Issue #36 (CR-036-6): Task-136 found five defects, each with the orchestrator's decision, applied spec-only. FR-098-AC-6/AC-7: structural parity on `records-and-scalars` cannot hold because the two frontends mint identities by different closed rules (FR-053 declaration name against FR-095 lowercase slug; author-named scalar against the minted alias `type/<DisplayName>.<fieldName>`; `diagnosticCode` prefixes), filed as filament-core-data#87; the case is recorded single-dialect with both source trees present and the reason, the projection materialises an absent `relationships`/`operations`/`clauses` as `[]` (the FR-028-CON-1 reading, not a widening), and TC-1290/TC-1291 are blocked on #87. FR-098-AC-8: `generate --target typescript` refuses every document carrying two kernel-scalar extensions, FR-046's own assurance output included, with `DUPLICATE_IDENTITY`, filed as #88; `--target rust` through the generic CLI is `BACKEND_NOT_IMPLEMENTED`, so the Rust half runs through `src/compiler/backends/rust-serde/cli.mjs` and the TypeScript half of TC-1292 is blocked on #88. FR-098-AC-4: "exactly its code and no second" became "exactly its code as the first blocking diagnostic" with necessary companion diagnostics pinned by the test; `KERNEL_NAME_SHADOWED`'s negative is refused with `DUPLICATE_TYPE_NAME` at lift level per FR-092-AC-8, so the non-blocking negatives are exactly two (`ENGINE_DIAGNOSTIC`, `ARTIFACT_NOT_LOWERED` legacy form). FR-098 inventory: added `clauses/`, `edges/`, `lower/`, `resolve/`, `registry-doc/`, `modules/{acme-other,frobnicates}`; `both-forms` lives at `negatives/ARTIFACT_NOT_LOWERED`; a fixture's `modules.json` `{"roots": [...]}` takes precedence over its own `modules/` and the defaults; constructed negatives carry no `expected/`. FR-095-AC-7: a `---` title fails earlier as `UNNAMEABLE_ARTIFACT`; `UNSLUGGABLE_NAME` is reached through an enumeration value `***`. Matrix: TC-1252, TC-1290..1292 retitled, TC-1290/1291 `blocked on issue #87`, TC-1292 TypeScript half `blocked on issue #88`; no row added or removed, every TC id stays on its AC. Composite index residues record #87 and #88 as new open items. No crate or plan file changes.
* **2026-09-09** — Issue #36 (CR-036-5): Task-135 found four wording defects in the command line and golden requirements, applied spec-only. FR-099: `lift --write-goldens` takes `--fixtures <dir>` (the inventory root, the only locator FR-099-CON-3 admits) and `--staging <dir>` (outside every bundle root and module root, since each fixture's `expected/` sits inside its bundle root, which FR-097 refuses as an output location), lifts into the staging directory and installs each `expected/` by rename; `extraction-frontend-goldens` names both options. `cargo audit` cannot read `deny.toml`, so `extraction-frontend-audit` runs `cargo +$(EXTRACTION_TOOLCHAIN) audit --deny yanked` and the licence policy stays with `extraction-frontend-deny`; AC-6 split by target. AC-3: a document missing `contractVersion` is defaulted to `1.0.0` by the reader and rejected at `/ir/source/dialect`, so the criterion keeps `INVALID_IR`, exit `1` without naming `contractVersion` in the rejection. FR-099 / FR-098: `--write-goldens` and `extraction-frontend-check` cover only the `expected/` directories beside a `spec/spec.md`; a constructed negative's `expected/diagnostics.json` is authored, neither regenerated nor diffed, and asserted by TC-1288 alone (FR-099 `lift` and Make-target behaviour, FR-098 Goldens, FR-098-AC-2). Matrix: TC-1286, TC-1297, TC-1349 titles rewritten and the TC-1297 state-table row restated; no row added or removed, every TC id stays on its AC. No crate or plan file changes.
* **2026-09-09** — Issue #36 (CR-036-7): Task-137 found two wording defects, applied spec-only. (1) NFR-031-AC-5 and FR-091-CON-2 placed the one `std::fs::read` of `<module root>/manifest.yaml` in `bundle.rs`; in code it is `write::read_manifest` called from `lift.rs`, and `write.rs` is the crate's only `std::fs` module (reads: exactly `<module root>/manifest.yaml`, the golden walk, `inspect --ir`; writes: the atomic outputs of FR-097), every other module `std::fs`-free — a stricter truth; NFR-031 Rationale, the files-opened metric, and the AC-5 exemption list restated to `write.rs` alone. (2) NFR-031-AC-7 and Verification asked for `unshare -n`, which unprivileged hosts cannot enter without a user namespace first; restated as `unshare -rn`, or an equivalent unprivileged network namespace. Matrix: TC-1304 and TC-1306 titles and the NFR-031 summary row restated; no row added or removed, every TC id stays on its AC. No crate or plan file changes.
* **2026-09-09** — Issue #36 (CR-036-8): Task-138 found four defects in NFR-032, each with the orchestrator's decision. (a) `test/rust-backend.test.ts` TC-742 (issue #21, NFR-023-AC-7) requires every crate in the workspace `Cargo.lock` to hold a row in the root `THIRD-PARTY-NOTICES.md`; the pinned `quire-rs` adds 182 crate names (188 package versions, `ahash` first) to the lock, so that suite was red on this branch while the root register was outside NFR-032's permitted set. Decision: NFR-032 permits additive rows in the root `THIRD-PARTY-NOTICES.md` for exactly the crates this change adds to `Cargo.lock` — a register that omits a locked crate is the defect NFR-023 forbids, and the widening is bounded to the additions the lock proves (Scope, traceability paragraph, two new metric rows, Verification, AC-1). (b) Verification and AC-7 name `scripts/extraction-frontend-harness.mjs` but the harness lived at `crates/extraction-frontend/scripts/`, where `test/compiler.test.ts` ("never imports a module under spikes/") fails it: every tracked `.mjs` naming `src/compiler` must live under the root `scripts/`, and the harness names `src/compiler/**` in its prohibited table. Decision: the harness moves to `scripts/extraction-frontend-harness.mjs` (`git mv`), NFR-032 permits exactly that one root `scripts/` path and prohibits the rest of `scripts/**`; the Verification text stands. (c) AC-4 admitted only `spec-bundle`/`reason` members and the added case; Task-136 also rewrote the top-level `$comment` whose FR-045 sentence reserved the column for this ticket — added to the admitted set (Scope, metric, AC-4). (d) Rationale records that `test/semantic-kernel.test.ts` (issue #11's NFR-030 gate) fails on this branch because it measures `main...HEAD`, filed as filament-core-data#89, and that this change does not edit that suite. Matrix: TC-1310 and TC-1313 titles and the NFR-032 summary row restated; no row added or removed, every TC id stays on its AC. Root `THIRD-PARTY-NOTICES.md` gains one section and 188 rows (additions only); the harness permitted list and TC-1299's outside-the-crate set gain the register with an additions-only check. FR-099-CON-1 / FR-099-AC-5 still state the outside-the-crate set as exactly four paths; reported for a follow-up CR, not edited here.
* **2026-09-09** — Issue #36 (CR-036-9): the post-review fix pass over SR-169 (code review) and SR-170 (gap analysis), each finding's disposition recorded in its review file. (a) FR-099-CON-1 / FR-099-AC-5 / TC-1299: the outside-the-crate change set is exactly six paths — the `members` line, `Cargo.lock`, the Makefile block, the additive `THIRD-PARTY-NOTICES.md` rows (CR-036-8), `docs/semantic-data-system/extraction-frontend-diagnostics.md`, and `scripts/extraction-frontend-harness.mjs` — beside the FR-098 shared-case paths and the ticket's own `spec/`, `plan/` and `reviews/` artifacts, which FR-098 and NFR-032 own. (b) NFR-032 Verification: while the sentinels sit in different commits the range's tip is `HEAD` (which must descend from the closing sentinel's commit), so the review and CR commits after that sentinel are measured; once squashed the pinned tip is used and the forms coincide at the merge. The harness `workingRange` implements it and TC-1299 is green at the tip. (c) FR-098-AC-4 and the inventory: the non-blocking negatives are `DECLARED_LOSS` and `ENGINE_DIAGNOSTIC` (`negatives/ARTIFACT_NOT_LOWERED/` is the blocking `both-forms` bundle; the legacy form lives under `legacy/`), and TC-1288 asserts the code is the first blocking diagnostic (SR-170 FND-1501). (d) FR-098-AC-8: the Rust half runs the backend's own writer `generateRust` through the harness `rust-generate` verb (SR-170 FND-1500); measured, the backend refuses the golden with `NAME_COLLISION` (its reserved `Uuid` re-export against the `UUID` kernel scalar), a backend defect outside this frontend reported for filing, so TC-1292 is blocked in both halves with each refusal measured by an un-ignored test. (e) FR-099 owns `lift --write-goldens --into <dir>` and the new `extraction-frontend-evidence` target that runs the `--ignored` static-evidence tests (SR-170 FND-1504, FND-1505). Matrix: 138 rows TC-1200..1350 `✅ passed`, 7 `✅ static evidence (make extraction-frontend-evidence)` (TC-1298, TC-1306, TC-1314, TC-1318, TC-1323, TC-1324, TC-1349), TC-1316 `🚧` on issue #89 and TC-1317 `🚧` because the revert rehearsal's scratch clone lacks the Python and generated-binding environment (48 pytest/vitest rows and `rust-check` fail there independent of the revert; a harness follow-up), TC-1290/1291/1337 unchanged, TC-1292 blocked in both halves; TC-1299, TC-1292 and TC-1347 titles restated, TC-1347 also on FR-093-AC-13 (FND-1506); the summary rows `✅ Complete` except FR-098; no row added or removed, every TC id stays on its AC. Crate: SR-169 FND-1490..1494, FND-1498, FND-1499 applied (`create_new` temp files, the engine's `parse_manifest`, operation loci over the engine's `OperationDecl` set, `harvest_edges` with no private target reduction, the kernel-scalar library refused at lift start, bounded `inspect --ir` and fenced `modules.json` roots, an exhaustive applicability match); FND-1495..1497 recorded as dispositions.
* **2026-09-09** — Issue #88 (CR-088-1): owner ruling option (a) — an extension `identity` is unique per node, not per list. The TypeScript backend's `admit.mjs` kept one document-wide identity set and applied it to every node's `extensions[]`, so any document carrying `ix://agent-ix/semantic-core/ext/kernel-scalar` on more than one definition was refused with `DUPLICATE_IDENTITY`, including FR-046's own `assurance` output. Spec-only: FR-068's rule-table row for `DUPLICATE_IDENTITY` and FR-050's matching row each gain the per-node sentence (an extension identity is unique within one node's `extensions[]` and within the document-level `extensions[]`, and is never entered into the declaration-identity set); FR-068 gains two behaviour bullets and AC-25 (three kernel-scalar definitions admissible with zero diagnostics; two `ext/doc` extensions on one field raise `DUPLICATE_IDENTITY` at `/ir/types/N/fields/M/extensions/1/identity`) and AC-26 (`generate --target typescript` over `test/fixtures/compiler/packages/assurance` exits zero with zero diagnostics). Matrix: TC-1355, TC-1356 added `🚧`; ERR-282, EC-162; FR-068 summary row updated. `conformance/diagnostic-codes.json` is unchanged (its "within each list" rule text is correct as written). TC-1292 and FR-098 are untouched; the TypeScript half of TC-1292 unblocks when the backend lands.
* **2026-09-09** — Issue #90 (CR-090-1): owner ruling option (a) — the Rust backend maps a kernel-scalar definition named for its own support type onto that support type. `mapping.mjs` seeded the type scope with the reserved crate names and mapped every `kind: scalar` definition to a newtype, so a `scalar: uuid` definition deriving `Uuid` collided with the crate's `Uuid` re-export and the lifted `config-version-table` document was refused with `NAME_COLLISION`; the same shape hits a package-local `Date`, `DateTime`, or `Duration`. Spec-only: FR-055 "Scopes and collisions" gains the mapping bullet (no newtype, every `typeRef` renders `crate::support::<Support>`, the existing re-export is the rendering) and the bullet keeping `NAME_COLLISION` for any other `scalar` or kind deriving a reserved name; AC-15 (the `UUID` case and its three siblings generate with zero diagnostics, the field renders `crate::support::Uuid`) and AC-16 (`scalar: string` named `Uuid`, and a record named `Date`, still raise `NAME_COLLISION` naming both identities). FR-062's branch-register section gains one `name-derivation` row per support scalar (`names.mjs` contribution), each bound to a case, with `cli.mjs register --check` required to pass after regeneration, and AC-15. Matrix: TC-1357, TC-1358, TC-1359 added `🚧`; ERR-283, EC-163; FR-055 and FR-062 summary rows updated. TC-1292 and FR-098 are untouched; the Rust half of TC-1292 unblocks when the backend lands.
* **2026-09-09** — Issue #87 (CR-087-1): one identity-minting rule shared by both frontends. Reason: the owner ruling on #87 (option a) asks for one rule stated once in `docs/semantic-data-system/contracts-v1.md` and implemented by both FR-053 and FR-095; the Phase 0 comment found three reasons to make FR-053's rule (which is FR-034's) the shared one — FR-034 and FR-053 are already one rule by FR-053-CON-1, adopting the spec-bundle rule would change the published kernel package's identities and `diagnosticCode`s (a compatibility change under FR-051) plus 40 compiler goldens and 4 conformance dirs, while adopting FR-053's changes only the extraction crate and its 12 goldens, and the case-preserving slug works for every Markdown-sourced name. Changes: `contracts-v1.md` gains `### Identity minting` under Semantic model (slots and parts, case-preserving `slug`, alias `type/<Name><Field>`, `diagnosticCode` form with camelCase not split, `DUPLICATE_IDENTITY` and `UNSLUGGABLE_NAME` rules, FR-034/FR-053/FR-095 named as its three implementations). FR-053: one sentence citing the contract section and FR-095; FR-095 added to Downstream. FR-034: one sentence citing the contract section beside the alias/`diagnosticCode` rule. FR-095: "Node identities" rewritten to the shared rule (verbatim `type/` name equals its slug for an `Identifier`; alias `type/<Name><Field>`; parameters under `field/<Name>-<operation>-<param>`, no `param/` slot; case-preserving slug; `diagnosticCode`; `DUPLICATE_IDENTITY` at the later path with the earlier locus in `related`; `Status`/`status` distinct); Outputs drop `param_identity` and add `diagnostic_code`, the collision check, and `test/fixtures/compiler/shared/identity-cases.json`; CON-4 (no byte of `src/compiler/**`, `packages/**`, `conformance/**`, `schema/**` changes); AC-6, AC-7, AC-14 rewritten; AC-16 added (shared identity table asserted by a Rust test and a node test); FR-034 and FR-053 join the upstream dependencies. FR-093: `DUPLICATE_TYPE_NAME` fires on equal verbatim `displayName` (no longer on equal lowercase slugs); the alias identity and `displayName` are `<DisplayName><Field>`; `diagnosticCode` is FR-095's shared form (`CONFIGVERSION_VERSIONNUMBER_MIN`, no case-boundary split, `SCREAMING_FIELD` removed); enumeration rows keep the equal-slug rule under the case-preserving slug; Rationale's alias paragraph restated; AC-4, AC-5, AC-12, AC-13 updated (`Status`/`status` moved to FR-095-AC-14's positive half; `versionNumber`/`version_number` now distinct codes). FR-094: parameter identity `field/<Name>-<op>-<param>` through `field_identity`; relationship, operation, clause patterns restated in the shared form; AC-11. FR-096: `DUPLICATE_IDENTITY` (error, blocking) joins the closed `Code` list, the severity table, and AC-2; `extraction-frontend-diagnostics.md` gains the row (27 codes). FR-098: Rationale records the ruling; inventory lists `negatives/DUPLICATE_IDENTITY`; `cases.json` records `records-and-scalars` with both dialects non-null; the parity comparison holds unconditionally; AC-6/AC-7 unconditional; AC-8 untouched (#88 and the rust-serde defect are separate). FR-099: the evidence target's skip list names TC-1292 only. Matrix: TC-1223, TC-1224, TC-1241, TC-1251, TC-1252, TC-1290, TC-1291, TC-1333, TC-1334, TC-1347 retitled and `🚧 pending the issue #87 implementation`; TC-1351 (`DUPLICATE_IDENTITY` negative fixture → FR-095-AC-14), TC-1352 (shared identity table, Rust side → FR-095-AC-16), TC-1353 (node side → FR-095-AC-16, FR-053-AC-6), TC-1354 (`git diff --stat origin/main -- src/compiler packages conformance schema` empty → FR-095-CON-4) added; ERR-261 and EC-154 rewritten, ERR-281 and EC-161 added; the FR-093/FR-094/FR-095/FR-098 coverage rows and the TC-1290/1291 permutation row and the FR-093-AC-13 boundary row restated; the Test Execution Summary counts are not recomputed here. Spec-only: no crate, fixture, golden, or plan file changes in this commit.
* **2026-09-12** — EPIC #100 Phase 2 (FR-130): the Rust/Serde backend is registered in the generation seam. The backend was complete and unreachable — `src/compiler/backends/seam.mjs` carried `rust` as `declaredUnimplemented`, so the documented entry point answered `state: "unavailable"` with a diagnostic naming issue #21 as still owing work it had already done. The gap was an interface mismatch, not a missing capability: the seam requires an object carrying `identity`, `version`, `supportedIrVersions`, `supportedFeatures` and `generate`, while the backend exposed `generateRust(request, sink)`. New: US-019 and FR-130, with TC-1388..TC-1395. The adapter is its own module, `rust-serde/backend.mjs`, rather than an export of `rust-serde/index.mjs`, because the latter holds every write the backend performs and the seam is imported by every generation path — FR-130-CON-2 pins that separation. Two findings the registration surfaced. (a) `src/compiler/cli.mjs` built every request's `backend` descriptor from the TypeScript backend unconditionally; `src/identity.rs` renders `GENERATOR_IDENTITY` from that member, so a generated Rust crate claimed the TypeScript backend produced it. The descriptor is now selected from the target the caller named, and TC-1391 pins it; the defect was unobservable while `typescript` was the only implemented target. (b) `test/compiler-core.test.ts` TC-746 asserted that the `rust` target returns `unavailable` — an assertion about issue #21's absence, which FR-063's own `registryWith` seam exists to prevent and which went red the moment this change landed; the arm now runs over a synthetic registration. The Rust backend declares `supportedIrVersions: ["1.1.0"]` through the seam while its own command keeps `["1.0.0", "1.1.0"]`, FR-063-CON-5 applied consistently: the frozen FR-041 prototype document also calls itself `1.0.0`. Nothing the backend emits changes — TC-1390 compares the seam's bytes to `generateRust`'s at every path and they are identical. Two vocabularies left unreconciled and recorded in US-019's notes: the two backends spell the same features differently (`kind:scalar` against `scalar`), and the seam's manifest and the backend's own manifest give `normalizedFingerprint` different meanings.
* **2026-09-12** — EPIC #100 requirement backing: NFR-039, FR-132 and FR-133, for three programme issues that traced to no requirement. NFR-039 (#92, #51) states that a change-set gate resolves both ends of its path set from history. The rationale is measured rather than argued: six gates were repaired in one sitting across two languages, each red or vacuous for a reason outside the change under test — two resolving `main...HEAD`, one resolving `merge-base HEAD main`, one comparing a frozen hunk to the working tree, and one reading a directory no commit contains. That last is a sixth face of the defect, and the one a range fix alone does not catch: a gate may pin its range correctly and still compare the historical hunk to the working tree, which silently converts "this is what the change did" into "no later ticket may edit these lines". FR-132 (#65) specifies the Python IR reader the conformance corpus's `python-backend` slot needs. The slot has been unavailable since #23, and the cause is a shape mismatch rather than a scheduling one: a package of generated types decides whether a value satisfies its own shape, while the oracle's readings are cross-field judgements over a resolved document. The requirement specifies a reader rather than forcing the generated package into the adapter's shape, which is the move that would make the slot report something while measuring nothing. FR-133 (#80) resolves a minted name against the Rust backend's reserved namespace. FR-083 mints `SourceLocusPath` for `SourceLocus.path` and FR-055 reserves the same identifier, so the Rust kernel crate cannot be generated at all; neither rule is wrong, and the resolution states which yields rather than relaxing the collision check. Matrix: TC-1410..TC-1421 added, all `🚧`, each naming its owning issue.
* **2026-09-12** — EPIC #100 gate backing: US-020, FR-134, FR-135 and NFR-040, closing the requirement-backing criterion for the programme's three remaining P0 gates. NFR-040 (#26) states that a published artifact identifies the source, compiler, IR, backend and schema it came from, and that a version combination is declared supported only where it has been measured against the conformance corpus — four things version independently here and nothing said which combinations were supported, so every combination was either implicitly supported with no evidence or implicitly unsupported, and neither was a position taken deliberately. FR-135 (#12) freezes the first contract major by a recorded human decision rather than by the absence of objections; its accepted findings each carry an owner and an expiry condition, so "we knew about it" stays distinguishable from "we decided about it". FR-134 (#6) retires the legacy Avro contract boundary by boundary, readers before writers, only where the reader count is zero. US-020 carries the story both answer, and states the asymmetry that shapes it: almost every migration mistake is undone by redeploying, but removing the last code that can read a persisted payload is recoverable only by restore. The census is therefore re-measured immediately before each removal rather than cited, because a census taken once and trusted afterwards is how a reader gets missed. FR-106 now names the presence half of #93 in its own text, rather than leaving the trace to be inferred from a matrix row — an issue whose requirement can only be found by recognising the subject matter traces to nothing a reader can follow. Matrix: TC-1422..TC-1433 added, all `🚧`, each naming its owning issue.
