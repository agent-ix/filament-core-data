---
type: log
title: "filament-core-data specification update log"
description: "Chronological log of structural changes to the requirements bundle."
---
# filament-core-data specification update log

## History

* **2026-08-29** — Initialized the semantic data architecture requirements bundle for issue #8.
* **2026-08-29** — Added the read-only Filament contract-census requirements for issue #10.
* **2026-08-29** — Added the pinned, isolated TypeSpec feasibility and structural-schema decision requirements for issue #4.
* **2026-08-30** — Added the issue #9 semantic IR, package, mapping, representation, generated-target, compatibility, and non-disruption requirements.
* **2026-09-03** — Issue #30: recorded the owner decision (TypeSpec is the structural source, ADR-0005). Removed the JSON Schema fallback and the "owns and funds the compensation" pass condition from FR-018 and US-004; retitled FR-018.
* **2026-09-03** — Issue #9: FR-019 and US-005 rewritten from the JSON Schema fallback to TypeSpec as the v1 structural source per ADR-0005; TC-199 resolved by the owner decision on issue #4. The 13 contract schemas are unchanged.
* **2026-09-03** — Issue #30 (follow-up): removed the remaining JSON Schema fallback mandate from FR-007 (behavior, AC-2), FR-008-AC-4, and US-002 (EX-1, AC-1); TC-026/TC-032/TC-036 retitled.
* **2026-09-03** — Issue #34 (Task-035): FR-030 manifest-target binding widened from the five-value generated-target enumeration to the declared registry (generated targets ∪ representation formats) because the frozen v1 manifest fixture selects `markdown`; TC-229/TC-230 retitled.
* **2026-09-03** — Issue #34: added US-006, FR-027 (multiplicity and units), FR-028 (relationships, operations, clauses), FR-029 (closed constraint vocabulary), FR-030 (source dialect and manifest target binding), NFR-013 (additive IR revision); amended FR-020 Behavior and AC-7/AC-8; spec.md scope updated for IR v1.1.
* **2026-09-03** — Issue #35: added US-007, FR-031 (declaration grammar in TypeSpec), FR-032 (kernel scalar library), FR-033 (JSON Schema projection with the #31 normalization pinned), FR-034 (lowering to IR v1.1 with zero loss; UCUM `UnitSymbol` carries SR-036 FND-113), NFR-014 (small kernel discipline); spec.md scope updated.
* **2026-09-03** — Issue #20: added US-008, FR-035 (conformance corpus, versioning, minimization, provenance), FR-036 (independent JSON-level semantic oracle), FR-037 (differential harness, adapter registry, divergence register), FR-038 (construct register with four case classes per row), FR-039 (coverage accounting, promotion thresholds, import API), NFR-015 (blessing-free deterministic evidence), NFR-016 (isolated corpus); spec.md scope updated. Ids were allocated against `main` at 8425a14; issue #27 had allocated none.
* **2026-09-03** — Issue #20 (review pass): SR-047..054 findings applied. The case input became an input bundle composing the published package, lock, mapping, profile, and consumer-policy schemas; expected diagnostics became `common.schema.json#/$defs/diagnostic` documents with the pointer as corpus metadata; a diagnostic-code register was added; the alias cycle and the depth bound were separated; `resultState` was made exhaustive and disjoint; the compatibility classification was scoped to the IR surface with a declared restrictiveness order; the harness lost its clock; the divergence register gained an owner and a verdict and a sibling contract-gap register; the `./conformance` package export was dropped to issue #11; and the construct register grew to 22 families with a justified `notApplicable` escape. TC rows became TC-280..341.
* **2026-09-03** — Issue #27: added US-009, FR-040 (prototype disposition inventory), FR-041 (promoted semantic-IR emitter and narrow build interface), FR-042 (TypeScript and Rust backends), FR-043 (governed Python JSON Schema adapter), FR-044 (frozen-spike replay with one declared evidence delta), NFR-017 (deterministic promoted compilation, including the retained Rust lockfile seeding fix), NFR-018 (non-disruptive promotion and clean rollback); spec.md scope updated. Ids skip US-008, FR-035..039 and NFR-015..016, which the parallel issue #20 conformance-corpus branch allocated first.
* **2026-09-04** — Issue #19: added US-010, FR-045 (frontend seam and dialect registry), FR-046 (TypeSpec lowering to contract IR 1.1.0 with the semantic decorator library), FR-047 (package graph resolution with exact JSON loci), FR-048 (lock, canonicalization, and v1 fingerprint), FR-049 (closed diagnostic registry, ordering, and limits), FR-050 (IR validation, normalization, and fingerprint), FR-051 (compatibility diff and IR schema-evolution projections), FR-052 (`compile`, `inspect`, and `diff` commands); NFR-019 (deterministic contract compilation), NFR-020 (bounded and safe compilation of untrusted inputs), NFR-021 (non-disruptive compiler core); spec.md scope updated. FR-046-CON-1 declares that the frozen FR-041 prototype IR is retained unchanged and the contract IR is a second lowering beside it, so no issue #4 golden is regenerated. Ids skip US-008, FR-035..039 and NFR-015..016, which the parallel issue #20 branch allocated first.
* **2026-09-04** — Issue #19 (follow-up): NFR-021 re-scoped from "the whole branch" to this change's own path set, fixed at both ends by history, after issue #20 measured the gate annexing a later ticket's paths and failing issue #19 for them; added NFR-021-AC-10 (a later change adds no path to this change's set) and TC-644. No permitted or prohibited path changed.
* **2026-09-04** — Issue #22: added US-012, FR-063 (generation backend seam and the TypeScript target contract), FR-064 (lower IR type definitions to TypeScript), FR-065 (ESM package and export surface), FR-066 (generated runtime validators), FR-067 (identity and fingerprint metadata), FR-068 (IR admissibility and declared loss), FR-069 (canonical form and IR-surface classification), FR-070 (the `typescript-backend` conformance adapter), FR-071 (the `generate` command and the surface fixtures); NFR-024 (portable, deterministic generated TypeScript), NFR-025 (non-disruptive TypeScript backend); spec.md scope updated. Ids were allocated as an exclusive range against `main` at c1b8807 because issues #21 and #23 are authored in parallel: US-012, FR-063..071, NFR-024..025, TC-745..844, SR-087..096, FND-1000..1099. GAP-011 is recorded as an open dependency on issue #9 and is not decided here.
* **2026-09-04** — Issue #22 (review pass): SR-087..SR-094 findings applied. The resolved type model gained an owner and a declared shape (FR-064 `buildModel`); the emitted file set grew to eight with a generated `errors.ts`; the `as` prohibition became a syntactic ban on type assertions with `as const` exempt; every bundler-dependent obligation became a static reachable-symbol walk, because no bundler is resolvable in this repository and both lockfiles are frozen; generated text is rendered through an injected formatter backed by the pinned `@biomejs/biome` binary, which is how the issue's deterministic-formatted-output deliverable is discharged; the generated fixture typechecks in its own program because the root `tsconfig.json` lacks `exactOptionalPropertyTypes` and sets `noUnusedLocals`; every claim that a corpus case supplies an instance payload was removed — the corpus carries none — and replaced by an authored instance corpus cross-checked against `ajv@8.20.0`; every whole-corpus absolute became this slot's own delta, because issues #21 and #23 regenerate the same accounts; FR-068 gained a suppression channel, a declared severity and locus reading, a per-code derivation ledger and a first-run divergence count; FR-069 gained the field and variant classification rules the published policy requires and the normative version-uplift round-trip rule; FR-070 gained the agreement obligation it lacked and a prohibition on registering any divergence; and NFR-025's three merge-base comparisons became history-pinned or tree assertions. Four gaps were filed rather than decided here: #61 (a diagnostic's severity and locus are derivable from no published artifact), #62 (the compiler's depth limit 128 against the corpus's 256), #63 (three concurrent backend tickets regenerate the same four shared machine-generated artifacts), and #64 (the normative version-uplift rule against the oracle's flat classification). `agent-ix/filament-core-data#9` is closed, so every citation naming it as a live decider now cites the gap row and #59.
* **2026-09-04** — Issue #23: added US-013, FR-072 (pinned toolchain and advisory floor), FR-073 (immutable per-family target profiles), FR-074 (owned schema-preparation pass, additive to FR-043), FR-075 (schema and option refusal guards over the five executable keys the pinned 0.76.0 actually binds), FR-076 (sandboxed deterministic runner outside `src/compiler/` so FR-043-AC-8 stays green), FR-077 (per-family qualification verdicts, probe corpus, and retained-gap register), FR-078 (generated-source inspection for silent `Any` and unconstrained-mapping degradation), FR-079 (package layout, provenance, and examples), FR-080 (strict static checking and falsifiable runtime validation); NFR-026 (sandboxed generation from untrusted schema), NFR-027 (reproducible and non-disruptive generation). Ids skip FR-054..FR-071 and NFR-022..NFR-025, which the parallel issue #21 and #22 backend branches allocated first. No published schema, fixture, corpus file, or issue #4 golden changes; nothing is published.
* **2026-09-04** — Issue #23 (review pass): SR-097..SR-104 findings applied. The adapter moved from a Node/Python split to one Python surface, because a Python runner cannot call a JavaScript guard before it spawns anything (FND-1101, FND-1156, FND-1166). `--extra-fields forbid` was removed from every profile: measured against the pinned generator, `additionalProperties: false` already closes both Pydantic families and `TypedDict`, and the blanket flag closes models the schema leaves open (FND-1115). `--field-constraints`, `--use-annotated`, and `--strict-nullable` were added because the `conint`/`constr` call form is not a valid annotation and fails strict type checking, and because a defaulted non-nullable field otherwise renders `T | None`. `--formatters builtin` was fixed as the value, removing black and isort from generation entirely (FND-1158). Host-observed versions left every byte-compared artefact and the toolchain fingerprint, which now covers only the declared pins, so the issue #42 coupling is not reproduced (FND-1127, FND-1137, FND-1157). FR-074 no longer runs FR-043's normalizer over published schemas, which would destroy the identities their cross-file `$ref`s resolve through (FND-1116), and the multi-document input mode is now stated (FND-1123, FND-1160). FR-078 gained a reporting mode so a lossy family can be measured rather than suppressed (FND-1111, FND-1139, FND-1178) and a stated attribution rule (FND-1122). The verdict vocabulary was closed to three values with `demonstrated profile` defined (FND-1112, FND-1113). FR-076's limits became stated values in `limits.json` (FND-1119, FND-1130). The corpus obligation was re-scoped: the `python-backend` slot's owning issue is #23, not #52, and it needs an IR reader a generated type package cannot be, so this change delivers a read-only advisory account and files the reader (FND-1133..FND-1136, FND-1168). NFR-027 took on converting the six suites of issue #51 rather than widening six permitted-path lists (FND-1155, FND-1170, FND-1171). ERR rows moved to ERR-132..143 after colliding with twelve merged rows (FND-1100, FND-1132). TC rows were re-mapped to the corrected criteria and retyped per SR-101 (FND-1144..FND-1150).
* **2026-09-04** — Issue #22 (merge integration): FR-069's variant-addition rule was self-contradictory — its Behavior said `conditional` where a policy admits unknown members and `breaking` otherwise, while FR-069-AC-17 said `conditional` with no policy at all, and neither matched `docs/semantic-data-system/compatibility.md`, the authority both cite. The requirement now states that document's rule — `additive` for an open-enum consumer, `breaking` for a closed one — and carries `VARIANT_ADDITION_POLICY`, a single named constant with two settings in the manner of `VERSION_UPLIFT_POLICY` and `REFERENCE_POLICY`, defaulting to the corpus's published `conditional` reading. Both settings were measured: 25 of 25 compatibility cases agree under `corpus`, 23 of 25 under `contract`, differing on exactly `ENUM-004` and `UNION-004`. The corpus is not moved here because FR-070 forbids resolving a disagreement by editing a corpus case and NFR-025 makes `conformance/cases/**`, `conformance/corpus.json`, `conformance/contract-gaps.json` and `conformance/divergences.json` prohibited paths; the disagreement is reported to the corpus's owner instead. FR-076-AC-12 and FR-076-CON-1 gained the named exemption for FR-071's injected formatter, which is the one module under `src/compiler/` that starts a child process by design and which no backend can reach — the Python gate TC-894 now carries the same named exemption and the same reachability assertion the JS gate already carried.
