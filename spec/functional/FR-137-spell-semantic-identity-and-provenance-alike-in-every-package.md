---
id: FR-137
title: "Spell semantic identity and provenance alike in every generated package"
type: FR
relationships:
  - target: "ix://agent-ix/filament-core-data/US-021"
    type: "implements"
  - target: "ix://agent-ix/filament-core-data/FR-065"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-056"
    type: "depends_on"
---
# FR-137: Spell semantic identity and provenance alike in every generated package

## Description

Every generated package SHALL name its semantic-identity artifact *identity* and
its provenance artifact *provenance*, and SHALL name neither of them *metadata*.

[ADR-0007](../../docs/semantic-data-system/adr/0007-emitted-set-contract.md)
declares the five conceptual outputs, rules that the emitted set is realised
idiomatically per language rather than fixed by file name, and records that the
`identity` / `provenance` swap between the Rust and TypeScript backends is a
defect rather than a per-language variation. This requirement performs that
repair. It is the only part of the emitted set stated as a spelling, because it
is the only part a consumer cannot reason around: every other difference between
the targets is explained by a language property, and a name is not.

Two targets already satisfy this and are changed by nothing here. The JSON
Schema emission carries semantic identity inline on each schema node as
`x-agent-ix-semantic-id` and its provenance in the index document's
`x-agent-ix-*` members; the Python package carries provenance as
`PROVENANCE.json` and delegates its types to a pinned generator. Both are
declared realisations under ADR-0007, not omissions, and both are named here so
that "every generated package" is a claim over a stated population rather than
over whichever targets happened to be inspected.

The repair moves names, not data. A generated package after this change carries
every datum it carried before, reachable by the same member names within each
artifact. Where a datum moves between artifacts it does so because the artifact
it sat in is being retired, and the requirement states where it lands rather
than leaving the move to an implementer.

This is a compatibility change under
[FR-051](./FR-051-classify-contract-compatibility.md) for a consumer that
imports a generated module by path, and is sequenced as one. It is not a
compatibility change in the contract the package describes.

## Inputs

- A semantic IR document accepted by a backend that emits a package of files
- The committed golden packages that assert each backend's emitted bytes
- The backend specifications that state where each of the five concepts lands

## Outputs

- `src/compiler/backends/rust-serde/crate.mjs`, emitting `src/provenance.rs` and
  `src/identity.rs` and no `src/metadata.rs`
- `src/compiler/backends/typescript-v1/metadata.mjs`, rendering the identity
  module and a separate provenance module
- `src/compiler/backends/typescript-v1/package-layout.mjs`, whose closed file set
  and fixed API surface name `provenance.ts` and `PROVENANCE`
- The re-cut golden packages under `test/fixtures/rust-serde/goldens/` and
  `test/fixtures/backends/typescript/expected/`

## Behavior

- The Rust backend SHALL emit its provenance constants in `src/provenance.rs`.
- The Rust backend SHALL emit its semantic identity, roles, relationships,
  operations, clauses, occurrences and extensions in `src/identity.rs`.
- The Rust backend SHALL NOT emit a module named `metadata`.
- The TypeScript backend SHALL emit the document's provenance and the
  fingerprint of its normalized form in `provenance.ts`, exported as
  `PROVENANCE`.
- The TypeScript backend SHALL emit the document-level and per-type contract
  data that `metadata.ts` carried in `identity.ts`, under the same export names.
- The TypeScript backend SHALL NOT emit a module named `metadata`.
- Each implemented backend's specification SHALL name where each of the five
  concepts of ADR-0007 lands in that target.
- No implemented target SHALL emit an artifact whose name denotes the concept
  another target's artifact of that name does not denote.

## Constraints

| ID | Constraint | Type | Validation |
|----|------------|------|------------|
| FR-137-CON-1 | Every datum a generated package carried before this change SHALL be present after it; the repair renames artifacts and moves declarations between them and removes nothing | Integrity | Test |
| FR-137-CON-2 | The TypeScript package's public export surface SHALL be unchanged except for `SEMANTIC_METADATA`, which is renamed `PROVENANCE`; no name is added and none other is removed | Compatibility | Test |
| FR-137-CON-3 | The generated bytes SHALL remain reproducible: regenerating a committed golden after this change SHALL produce the golden's bytes exactly | Determinism | Test |
| FR-137-CON-4 | The JSON Schema and Python realisations SHALL be declared rather than changed, because neither carries the defect | Scope | Inspection |

## Acceptance Criteria

| ID | Criteria | Verification |
|----|----------|--------------|
| FR-137-AC-1 | A generated Rust crate emits `src/provenance.rs` carrying the provenance constants, and emits no module carrying them under another name | Test (TC-1537) |
| FR-137-AC-2 | A generated Rust crate emits `src/identity.rs` carrying the semantic identity data, and emits no `src/metadata.rs` | Test (TC-1538) |
| FR-137-AC-3 | A generated TypeScript package emits `provenance.ts` exporting `PROVENANCE`, and emits no `metadata.ts` | Test (TC-1539) |
| FR-137-AC-4 | Every export the retired `metadata.ts` declared is declared by a module of the generated package after the change, under the same name | Test (TC-1540) |
| FR-137-AC-5 | No generated package of any implemented target emits an artifact whose name denotes `metadata` | Test (TC-1541) |
| FR-137-AC-6 | Each implemented backend's specification names where all five ADR-0007 concepts land in its target | Test (TC-1542) |
| FR-137-AC-7 | Regenerating each committed golden reproduces its bytes exactly under the renamed layout | Test (TC-1543) |
| FR-137-AC-8 | The generated TypeScript package's export surface equals its surface before the change, name for name, with `SEMANTIC_METADATA` replaced by `PROVENANCE` | Test (TC-1544) |

## Dependencies

- **Upstream**: [US-021](../usecase/US-021-read-a-generated-package-without-knowing-its-language.md) reading a generated package without knowing its language; [FR-065](./FR-065-generate-the-esm-package-and-export-surface.md) the ESM package's closed file set; [FR-056](./FR-056-emit-the-generated-rust-crate.md) the emitted crate's module set
- **Downstream**: [FR-067](./FR-067-generate-identity-and-fingerprint-metadata.md) the TypeScript identity and provenance renderers; [FR-085](./FR-085-generate-the-kernel-typescript-package.md) and [FR-086](./FR-086-generate-the-kernel-rust-crate.md) the kernel packages that carry the renamed layout
