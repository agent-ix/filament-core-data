---
id: FR-095
title: "Mint package identity, envelope digests, node identities, and provenance"
type: FR
relationships:
  - target: "ix://agent-ix/filament-core-data/US-015"
    type: "implements"
  - target: "ix://agent-ix/filament-core-data/FR-091"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-019"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-020"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-021"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-030"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/FR-048"
    type: "depends_on"
  - target: "ix://agent-ix/filament-core-data/NFR-031"
    type: "constrained_by"
  - target: "ix://agent-ix/filament-core-data/NFR-032"
    type: "constrained_by"
  - target: "ix://agent-ix/filament-core-data/NFR-033"
    type: "constrained_by"
---
# [FR-095] Mint package identity, envelope digests, node identities, and provenance

## Description

The extraction frontend SHALL derive the IR document's `source` and `package`
envelope, every node identity, and the provenance sidecar from loaded bundle
bytes, loaded manifest bytes, and pinned crate metadata alone, so that every
value in the emitted document traces to a source byte or a manifest value and
none is inferred from the host.

## Rationale

`source.dialect` is `spec-bundle` because the Markdown bundle is the authored
source and the extraction record is not a projection of it; this is the
declared reading of issue #77, cited here, and it changes no byte of
`schema/**`. The identity grammar is FR-020's; the `spec-bundle` value is
FR-030's. The quire-rs pin is a git `rev` (at or after `a874fb6`, quire-rs#411)
because no release tag contains it; the pin moves to a tag under quire-agent-c's
sweep (quire-rs#417), and the provenance golden is regenerated by
`--write-goldens` as one deliberate commit per pin move (FR-098-CON-2).

## Inputs

- The bundle's `spec.md` frontmatter `org`, `name`, and optional `version`, already refused by FR-091 (`BUNDLE_UNIDENTIFIED`) when absent or not matching `packageIdentity`
- Every loaded document's bundle-root-relative path and raw bytes, from FR-091
- Each loaded module's manifest bytes, `name`, `version`, and `semantic.contract_version`
- The root `Cargo.lock`, embedded at build time with `include_str!`, for the `quire-rs` version and git revision NFR-033 pins and the frontend crate's own version
- The `displayName` FR-093 fixes for each record, and the kernel scalar name FR-092 fixes for each scalar definition

## Outputs

- `crates/extraction-frontend/src/identity.rs`: `PackageIdentity`, `slug(name) -> Result<String, Unsluggable>`, and the `type_identity`, `field_identity`, `constraint_identity`, `relationship_identity`, `operation_identity`, `param_identity`, `variant_identity`, `clause_identity` minters
- `crates/extraction-frontend/src/envelope.rs`: `source_block(bundle)`, `package_block(bundle, modules)`, `provenance_record(bundle, modules)`
- The provenance record, written by FR-097 as the `<out>.provenance.json` sidecar on every successful lift

## Behavior

### The `source` block

- The frontend SHALL set `source.identity` to `ix://<org>/<name>/spec`.
- The frontend SHALL set `source.dialect` to `spec-bundle`.
- The frontend SHALL set `source.version` to the `spec.md` frontmatter `version` where present and to `0.0.0` otherwise.
- The frontend SHALL set `source.digest` to `sha256:<hex>` over the byte sequence formed, for each loaded document in path order under code-point comparison, by the document's bundle-root-relative path bytes, one `0x00` byte, the document's raw bytes, one `0x00` byte.

### The `package` block

- The frontend SHALL set `package.identity` to `<org>/<name>`.
- The frontend SHALL set `package.version` to the same value as `source.version`.
- The frontend SHALL set `package.manifestDigest` to `sha256:<hex>` over the manifest bytes of the module declaring the bundle's object types; where more than one module declares them, over the concatenation of their manifest bytes in module-name order.
- The frontend SHALL set `package.mappingVersions` to the one-element list holding the module's `semantic.contract_version`.
- The frontend SHALL set `package.profileVersions` to `[]`.
- The frontend SHALL set `package.lockDigest` to `sha256:<hex>` over the lines `<module package>@<module version>:<manifest sha256>`, one per loaded module, sorted by code point and each terminated by `\n`.
- The frontend SHALL emit `occurrences: []` and top-level `extensions: []`.

### Node identities

- The frontend SHALL mint every node identity from the closed pattern list below and from no other pattern:
  - type: `ix://<org>/<name>/type/<DisplayName>`, where `<DisplayName>` is the record's `displayName` verbatim, or the kernel scalar's name verbatim for a scalar definition (the same rule FR-046 applies)
  - field: `ix://<org>/<name>/field/<record-slug>-<field-slug>`
  - constraint: `ix://<org>/<name>/constraint/<record-slug>-<field-slug>-<keyword>`
  - relationship: `ix://<org>/<name>/relationship/<record-slug>-<verb>-<target-slug>`
  - operation: `ix://<org>/<name>/operation/<record-slug>-<op-slug>`
  - operation parameter: `ix://<org>/<name>/param/<record-slug>-<op-slug>-<param-slug>`
  - enumeration variant: `ix://<org>/<name>/variant/<enum-slug>-<value-slug>`
  - clause: `ix://<org>/<name>/clause/<record-slug>-<clause-slug>`
- The frontend SHALL compute every `<…-slug>` segment as the source name lowercased, every run of non-alphanumeric characters replaced by one `-`, with no leading or trailing `-`.
- The frontend SHALL use the slug, never the verbatim name, in every identity segment other than `type/`.
- If a name slugs to the empty string, then the frontend SHALL raise `agent-ix.extraction-frontend.UNSLUGGABLE_NAME` at the declaration's locus, blocking.
- The frontend SHALL mint identities that match `common.schema.json#/$defs/semanticIdentity`.
- The frontend SHALL treat two records whose slugs are equal as one name for the purpose of FR-093's `DUPLICATE_TYPE_NAME`, so that `Status` and `status` are refused at the second document rather than minting colliding `field/`, `constraint/`, `relationship/`, `operation/`, `param/`, and `clause/` identities.

### Provenance

- The frontend SHALL produce a provenance record carrying: `source.identity`; for each loaded module its `name`, `version`, and manifest `sha256`, in module-name order; the `quire-rs` crate version and git revision that the embedded `Cargo.lock` pins; the vendored semantic-core version; and the frontend crate version from the same embedded `Cargo.lock`.
- The frontend SHALL obtain the `quire-rs` revision and its own version only from the embedded `Cargo.lock`, never from `env!`, `option_env!`, or a process environment read.
- The frontend SHALL omit the bundle root and module root paths from the provenance record; the record names a module by `name`, `version`, and manifest digest, which is how a test joins it to the vendored module's `PROVENANCE.json` revision.
- The frontend SHALL write no clock reading, hostname, username, absolute path, or environment value into the provenance record, per FR-019-CON-1.

## Constraints

| ID | Constraint | Type | Validation |
|---|---|---|---|
| FR-095-CON-1 | The frontend SHALL compute every value in the emitted `source` and `package` blocks and in the provenance record as a pure function of loaded document bytes, loaded manifest bytes, and the embedded `Cargo.lock`. | Determinism | Test |
| FR-095-CON-2 | The frontend SHALL NOT read the bundle's git history, tags, or remotes to mint a version, digest, or revision. | Determinism | Static analysis |
| FR-095-CON-3 | The frontend SHALL NOT widen `frontendDialect`; `spec-bundle` is the only value it stamps. | Compatibility | Test |

## Acceptance Criteria

| ID | Criteria | Verification |
|---|---|---|
| FR-095-AC-1 | The `config-version-table` fixture lifts with `source.identity` `ix://agent-ix/config-service/spec`, `source.dialect` `spec-bundle`, and `package.identity` `agent-ix/config-service`. | Test (TC-1246) |
| FR-095-AC-2 | A `spec.md` with `version: 2.1.0` yields `source.version` and `package.version` `2.1.0`; one without yields `0.0.0`. | Test (TC-1247) |
| FR-095-AC-3 | `source.digest` equals a SHA-256 computed by `sha256sum` over the stated path/NUL/bytes/NUL recipe assembled outside the crate, and changing one byte of one document changes it. | Test (TC-1248) |
| FR-095-AC-4 | `package.manifestDigest` equals the `sha256sum` of the module manifest bytes, and `lockDigest` equals the `sha256sum` of the sorted `<package>@<version>:<sha256>\n` lines, each computed outside the crate. | Test (TC-1249) |
| FR-095-AC-5 | `mappingVersions` is `["1.0.0"]` for the vendored spec-objects-business module and `profileVersions` is `[]`; `occurrences` and top-level `extensions` are `[]`. | Test (TC-1250) |
| FR-095-AC-6 | Every identity in the emitted `business` fixture document matches exactly one pattern of the closed list, including `param/` and `variant/`, and matches `semanticIdentity`, asserted by regex over every node. | Test (TC-1251) |
| FR-095-AC-7 | `slug("Config Version")` is `config-version`, `slug("A__B--C")` is `a-b-c`, `slug("--")` is `Unsluggable`, and an artifact titled `"---"` raises `UNSLUGGABLE_NAME` at its frontmatter, blocking. | Test (TC-1252) |
| FR-095-AC-8 | Lifting the same checkout twice from two different working directories and two different `HOME` values yields documents and provenance records byte-identical to each other and to the committed `expected/` golden. | Test (TC-1253) |
| FR-095-AC-9 | The provenance record names the `quire-rs` version and git revision that `Cargo.lock` pins and the frontend crate version, each asserted against the lock file read by the test, and the revision is at or after `a874fb6`. | Test (TC-1254) |
| FR-095-AC-10 | A pattern scan over the provenance record and the document finds no absolute path, no ISO 8601 timestamp, no hostname, and no username, including when the lift is invoked with an absolute `BUNDLE=` path. | Test (TC-1255) |
| FR-095-AC-11 | The `objects-extra` module loaded beside the vendored module yields `manifestDigest` over the two manifests concatenated in module-name order, and swapping the order of the caller's module roots does not change it. | Test (TC-1256) |
| FR-095-AC-12 | A grep of `crates/extraction-frontend/src/` finds no `git2`, no `Command::new("git")`, no `std::env::var`, no `env!`, and no `option_env!`; planting `env!("CARGO_PKG_VERSION")` in `envelope.rs` fails the gate. | Static (TC-1257) |
| FR-095-AC-13 | The emitted envelope of every positive fixture passes `agent_ix_semantic_ir::decide` at lift time with zero `INVALID_IR` diagnostics. | Test (TC-1258) |
| FR-095-AC-14 | Two records titled `Status` and `status` slug to the same `status`, and lifting a bundle holding both refuses at the second document with `DUPLICATE_TYPE_NAME` and mints no `field/status-*` identity twice. | Test (TC-1347) |
| FR-095-AC-15 | The provenance record's module entry for the vendored spec-objects-business module carries the manifest `sha256` that `fixtures/modules/spec-objects-business/PROVENANCE.json` records for revision `d1840b8`. | Test (TC-1348) |

## Dependencies

- **Upstream**: [FR-091](./FR-091-read-a-spec-bundle-through-the-extraction-contract.md), [FR-019](./FR-019-select-v1-structural-source-and-ir.md), [FR-020](./FR-020-define-semantic-type-system-and-identity.md), [FR-021](./FR-021-define-package-graphs-exports-and-locks.md), [FR-048](./FR-048-build-and-verify-the-lock-and-fingerprint.md), [FR-030](./FR-030-bind-source-dialect-and-manifest-targets.md)
- **Downstream**: [FR-093](./FR-093-lower-field-declarations-to-ir-fields.md), [FR-094](./FR-094-lower-relationships-operations-and-clauses.md), [FR-097](./FR-097-normalize-validate-and-write-the-lifted-document.md), [FR-098](./FR-098-prove-fixture-goldens-and-cross-frontend-parity.md), [FR-099](./FR-099-provide-the-extraction-frontend-command-line.md)
- **Constrained by**: [NFR-031](../non-functional/NFR-031-deterministic-and-hermetic-lifting.md), [NFR-032](../non-functional/NFR-032-non-disruptive-extraction-frontend.md), [NFR-033](../non-functional/NFR-033-qualified-toolchain-and-licensed-dependencies.md)
