{
	"$comment": "The declared implementations the harness judges. A slot exists whether or not it is runnable today: an absent backend is an unmet row in the coverage account, never a pass. Supplying the command and an adapter-result emitter is the owning issue's obligation, not issue #20's; issue #20 declares the slot and the result contract.",
	"ownership": "Each owning issue supplies its adapter command and an emitter that writes one conformance/schema/adapter-result.schema.json document per case to stdout as a JSON array. Issue #20 declares the slots, the result contract, and the comparison; it does not implement any adapter.",
	"adapters": [
		{
			"id": "compiler-frontend",
			"language": "typescript",
			"owningIssue": "agent-ix/filament-core-data#19",
			"status": "unavailable",
			"pointerCompatible": true,
			"rationale": "The TypeSpec frontend and IR compiler core are not implemented; the prototype under spikes/ emits a document the published schema rejects (conformance/defects.json DEF-PROTO-001)."
		},
		{
			"id": "rust-backend",
			"language": "rust",
			"owningIssue": "agent-ix/filament-core-data#21",
			"status": "available",
			"command": [
				"cargo",
				"run",
				"--quiet",
				"--offline",
				"--locked",
				"--manifest-path",
				"../crates/conformance-adapter/Cargo.toml",
				"--"
			],
			"pointerCompatible": false,
			"rationale": "No Rust crate exists in this repository. pointerCompatible is false because the published sourceLocus path pattern cannot compile under RE2 (conformance/contract-gaps.json GAP-002), so a Rust reader is expected to locate by its own scheme."
		},
		{
			"id": "typescript-backend",
			"language": "typescript",
			"owningIssue": "agent-ix/filament-core-data#22",
			"status": "available",
			"pointerCompatible": true,
			"command": ["node", "adapters/typescript-backend/adapter.mjs"],
			"rationale": "Issue #22 landed the TypeScript generation backend under src/compiler/backends/typescript-v1/. The adapter answers every case from that backend's own decision modules — admitIr for the result state and the diagnostics, normalizeIrForTarget for the canonical form, classifySurface for a compatibility case's classification — and calls neither oracleVerdict nor compare. pointerCompatible stays true because the backend locates by the same RFC 6901 scheme this oracle uses."
		},
		{
			"id": "python-backend",
			"language": "python",
			"owningIssue": "agent-ix/filament-core-data#23",
			"status": "available",
			"pointerCompatible": true,
			"rationale": "Issue #23's adapter answers every case from this repository's own Python reader, tests/semantic_ir_reader.py, for the cross-field diagnostics and the normalized bytes, from the published v1 schemas for the schema layer, and from its own statement of the contract's compatibility rules for a classification. It imports nothing under conformance/oracle/ and cannot: the import surface is an ES module and this adapter is Python, which makes accidental transcription impossible rather than merely prohibited. Cases arrive materialized from tools/materialize-cases.mjs, so a patch-application defect cannot present as a reader divergence. pointerCompatible stays true because it locates by the same RFC 6901 scheme this oracle uses. The command runs through poetry because the reader's schema layer needs the project's pinned jsonschema, and a bare interpreter resolves whichever one the machine happens to carry.",
			"command": [
				"poetry",
				"run",
				"python",
				"adapters/python-backend/adapter.py"
			]
		}
	]
}
