# generated by datamodel-codegen:
#   filename:  legacy-adapter.schema.json

from __future__ import annotations

from enum import Enum
from typing import Annotated, Literal

from pydantic import ConfigDict, Field
from pydantic.dataclasses import dataclass

from . import common_schema


class Preservation(Enum):
    byte_lossless = 'byte-lossless'
    structure_lossless = 'structure-lossless'
    semantic_lossless = 'semantic-lossless'
    declared_lossy = 'declared-lossy'


type RetirementPrerequisite = Annotated[str, Field(min_length=1)]


@dataclass(config=ConfigDict(extra='forbid'))
class VersionedContract:
    identity: common_schema.SemanticIdentity
    version: common_schema.Semver


@dataclass(config=ConfigDict(extra='forbid', regex_engine="python-re"))
class FilamentLegacyAdapterContractV1:
    contractVersion: Literal['1.0.0']
    diagnostics: list[common_schema.Diagnostic]
    identity: common_schema.SemanticIdentity
    omissions: list[common_schema.SemanticIdentity]
    preservation: Preservation
    retirementPrerequisites: Annotated[
        list[RetirementPrerequisite], Field(min_length=1)
    ]
    sourceContract: VersionedContract
    targetContract: VersionedContract
