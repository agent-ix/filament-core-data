{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "ref-recursion",
	"construct": "recursion through a self-reference",
	"note": "A self-referential type must resolve, not flatten or recurse without bound.",
	"detector": {
		"kind": "regex",
		"pattern": "c:.*\\bN\\b"
	},
	"schema": {
		"$defs": {
			"N": {
				"type": "object",
				"title": "N",
				"properties": {
					"n": {
						"type": "string"
					},
					"c": {
						"type": "array",
						"items": {
							"$ref": "#/$defs/N"
						}
					}
				},
				"required": [
					"n"
				],
				"additionalProperties": false
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": true,
		"typed_dict": true,
		"msgspec_struct": true
	}
}
