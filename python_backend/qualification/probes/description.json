{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "description",
	"construct": "a description",
	"note": "Authored documentation must reach the generated source or the package is undocumented.",
	"detector": { "kind": "regex", "pattern": "who produced it" },
	"schema": {
		"$defs": {
			"P": {
				"type": "object",
				"title": "P",
				"description": "A provenance-bearing shape.",
				"properties": {
					"producer": { "type": "string", "description": "who produced it" }
				},
				"required": ["producer"],
				"additionalProperties": false
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": false,
		"typed_dict": false,
		"msgspec_struct": true
	}
}
