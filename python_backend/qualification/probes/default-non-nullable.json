{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "default-non-nullable",
	"construct": "a defaulted non-nullable field",
	"note": "A field the schema declares non-nullable must not acquire `None` from having a default.",
	"detector": { "kind": "regex", "pattern": "v:\\s*str = 'z'" },
	"schema": {
		"$defs": {
			"D": {
				"type": "object",
				"title": "D",
				"properties": {
					"v": { "type": "string", "default": "z" }
				},
				"additionalProperties": false
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": true,
		"typed_dict": false,
		"msgspec_struct": false
	}
}
