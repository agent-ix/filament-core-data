{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "ref-local",
	"construct": "local reference",
	"note": "A `$ref` to a sibling definition must render as that type, not as its expansion.",
	"detector": {
		"kind": "regex",
		"pattern": "b:\\s*(\\\"?B\\\"?|B)"
	},
	"schema": {
		"$defs": {
			"A": {
				"type": "object",
				"title": "A",
				"properties": {
					"b": {
						"$ref": "#/$defs/B"
					}
				},
				"required": [
					"b"
				],
				"additionalProperties": false
			},
			"B": {
				"type": "object",
				"title": "B",
				"properties": {
					"x": {
						"type": "string"
					}
				},
				"required": [
					"x"
				],
				"additionalProperties": false
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": true,
		"typed_dict": true,
		"msgspec_struct": true
	}
}
