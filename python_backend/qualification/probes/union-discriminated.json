{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "union-discriminated",
	"construct": "discriminated union",
	"note": "The discriminator must reach the generated type; without it a consumer must guess the branch.",
	"detector": { "kind": "regex", "pattern": "(discriminator|tag_field)" },
	"schema": {
		"$defs": {
			"S": {
				"title": "S",
				"oneOf": [{ "$ref": "#/$defs/C" }, { "$ref": "#/$defs/Q" }],
				"discriminator": {
					"propertyName": "kind",
					"mapping": { "circle": "#/$defs/C", "square": "#/$defs/Q" }
				}
			},
			"C": {
				"type": "object",
				"title": "C",
				"properties": {
					"kind": { "const": "circle" },
					"r": { "type": "number" }
				},
				"required": ["kind", "r"],
				"additionalProperties": false
			},
			"Q": {
				"type": "object",
				"title": "Q",
				"properties": {
					"kind": { "const": "square" },
					"s": { "type": "number" }
				},
				"required": ["kind", "s"],
				"additionalProperties": false
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": false,
		"typed_dict": false,
		"msgspec_struct": true
	}
}
