{
	"$comment": "Issue #23, FR-077. One construct, one detector, one expected retention per family. The expectation is read from the contract, not from the measurement (FR-077-CON-2).",
	"id": "closure-unevaluated",
	"construct": "object closure by unevaluatedProperties",
	"note": "The form the official TypeSpec emitter actually writes; retained only because the FR-074 pass rewrites it.",
	"detector": {
		"kind": "regex",
		"pattern": "(forbid|closed=True|forbid_unknown_fields)"
	},
	"schema": {
		"$defs": {
			"K": {
				"type": "object",
				"title": "K",
				"properties": {
					"a": {
						"type": "string"
					}
				},
				"required": [
					"a"
				],
				"unevaluatedProperties": {
					"not": {}
				}
			}
		}
	},
	"expected": {
		"pydantic_v2_basemodel": true,
		"pydantic_v2_dataclass": true,
		"stdlib_dataclass": false,
		"typed_dict": true,
		"msgspec_struct": false
	}
}
