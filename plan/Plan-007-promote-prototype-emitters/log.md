---
type: log
title: "Plan-007 — Update Log"
description: "Chronological log of changes to the Plan-007 bundle."
---
# Plan-007 — Update Log

## History

* **2026-09-03** — Plan created from the validated issue #27 specification (US-009, FR-040..044, NFR-017, NFR-018, TC-320..397, SR-055..062) with eight tasks: guards first, the emitter on the critical path, the backends and the Python adapter in parallel, the spike rewiring as the join, the inventory last because it must account for files the earlier tasks create, then the cross-cutting gates and the review. Ids skip Task-048..059 and Plan-007's TC block starts at TC-320, leaving the earlier ranges to the parallel issue #20 branch.
* **2026-09-03** — Tasks 060–066 done. `src/compiler/` holds the promoted emitter (`ir.mjs`, `compile.mjs`, `emitters/semantic-ir/`), the TypeScript and Rust backends with their shared substitution helpers, the Python schema adapter and its pins, the six-symbol `index.mjs` with hand-written `index.d.mts`, `cli.mjs`, and `inventory.json` (14 components, 4 authored files). All four committed issue #4 goldens reproduce byte-for-byte: the semantic IR through all three routes (programmatic, CLI, `tsp --emit` by absolute path with `--option`), the TypeScript and Rust sources, and the Python input schema. The spike runner imports the promoted backends, shells the CLI with the historical generator identity, and seeds the committed `Cargo.lock`; the emitter package and its `file:` devDependency are gone and `pnpm-lock.yaml` carries no `file:`/`link:` specifier. Exactly one retained-evidence field changed (`evidence/custom.json` `command`). Two ambient inputs became explicit during implementation: `baseDir` replaces `process.cwd()`, and a code-point comparison replaces `localeCompare` — both verified to reproduce the golden order. Five pre-existing changed-path guards (issues #8, #9, #10, #34, #35) were scoped: each asserted invariants true only of its own branch, and #34/#35 additionally prohibited `package.json` and `pnpm-lock.yaml`, which the `file:` removal must touch. vitest 171/172, lint/build/typecheck green. Two pre-existing main defects filed: #42 (retained evidence is not reproducible off the minting workstation) and #43 (`packages/semantic-core` lost `private: true` in #40, leaving TC-254 red). TC-370 and TC-382 stay blocked on #42. Task-067 (review, gap analysis, PR) remains.
