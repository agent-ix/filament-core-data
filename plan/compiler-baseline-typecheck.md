# Compiler baseline typecheck recovery

Base: `bac1157370ecb6088d3934ca16a25899a8c406d4`.

This later compiler-maintenance change owns the existing root typecheck defects;
it is not the original #11 kernel packaging change and does not widen that
change's prohibited-path list. FR-066/FR-065 own the zero-field-record emitter
repair; FR-085 consumes a regeneration. The historical FR-085 blocked record
remains historical until this followup has verified the unchanged strict gate.

1. Give an empty generated field-name register an explicit string element type.
   Regenerate the kernel through its owning script; never patch generated bytes.
2. Compile freshly generated zero-field records under strict,
   exactOptionalPropertyTypes, noUnusedLocals and noUnusedParameters. Remove the
   annotation in memory as a negative control and require TS7034/TS7005.
3. Remove duplicate test imports, supply the existing Rust writer interface,
   construct a typed generation request and preserve readonly backend results.
   Record diagnostic codes without falsely claiming partial loss diagnostics
   satisfy the complete compiler diagnostic interface.
4. Run root typecheck and focused tests; report unrelated baseline gate defects
   without widening historical path ownership or changing corpus expectations.

No oracle, disputed corpus expectation, application migration, kernel mapping,
Rust name-collision repair or Python kernel implementation is in this change.
