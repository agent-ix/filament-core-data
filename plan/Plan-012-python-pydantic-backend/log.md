---
type: log
title: "Plan-012 — Update Log"
description: "Chronological log of changes to the Plan-012 bundle."
---
# Plan-012 — Update Log

## History

* **2026-09-04** — Plan created from the corrected issue #23 specification (US-013, FR-072..080, NFR-026..027, TC-845..944, SR-097..104) with thirteen tasks. Two of them exist because the review found the ground was not what the specification assumed. Task-081 converts the six merged suites that still baseline their changed-path gates on a moving ref: with those unconverted, any new directory this bundle adds makes them red, and the only alternative is widening six permitted-path lists, which issue #55 records as how those guards were disabled incrementally in the first place. Task-088 replaces the corpus obligation the first draft delegated to issue #52: the `python-backend` slot's owning issue is #23, and it needs an IR reader emitting contract diagnostics, which a package of generated types cannot be — so the bundle produces a read-only account and files the reader rather than claiming a blocker.
* **2026-09-04** — Tasks 080–091 done. The generator is qualified, not replaced: five declared profiles, one owned `unevaluatedProperties` rewrite, two refusal guards over the five executable keys the pinned 0.76.0 actually binds, a sandboxed runner, an `ast` inspection in two modes, twenty probes, and three emitted packages. Verdicts: `pydantic_v2_basemodel`, `pydantic_v2_dataclass`, and `msgspec_struct` qualified-with-conditions; `stdlib_dataclass` and `typed_dict` not-qualified, declared and measured rather than omitted. Three decisions the measurement made against the first draft: the preparation pass exists because the official emitter's `unevaluatedProperties` opens every sealed type in every family; `--extra-fields` stays out because it closes models the schema leaves open; and `--use-missing-sentinel` stays out because the only option that separates absent from null renders a value strict `mypy` rejects — recorded as a gap rather than papered over. The corpus obligation was re-scoped honestly: the slot is #23's, it needs an IR reader a type package cannot be, so the account reports 70 of 111 decided and agreeing, 41 undecidable, 0 over-strict, and all 111 rows unmet, with the reader filed as #65.
* **2026-09-04** — Three-state verification. Branch: 421/421 vitest, 495/495 pytest, `make lint` clean. Squash-merged with `origin/main` repointed and both the diff and the status empty: identical. That clone with a real unrelated sibling commit on top: identical. The third state earned its place — it found four instances of the accreting range in this bundle's own gates, one in TypeScript and three in Python, each attributing the sibling's `conformance/README.md` to a ticket that changes nothing under `conformance/`. All four now pin both ends to history, and `tests/change_range.py` is the Python mirror of `changeRange`. Falsified in four directions and perturbed with a prohibited edit inside the change's own range, which both languages name.

