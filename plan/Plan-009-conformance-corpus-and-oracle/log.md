---
type: log
title: "Plan-009 — Update Log"
description: "Chronological log of changes to the Plan-009 bundle."
---
# Plan-009 — Update Log

## History

* **2026-09-03** — Plan created from the validated issue #20 specification (US-008, FR-035..039, NFR-015, NFR-016, TC-280..319 and TC-622..419, SR-047..054) with nine tasks: schemas and guards, corpus machinery, the oracle on the critical path, two parallel case-authoring tasks, the harness beside them, coverage and import API as the join, determinism evidence, and the review gate.
* **2026-09-03** — Tasks 048–055 done. `conformance/` carries 4 contract-valid input bundles, 111 cases across 22 construct-register families (26 positive, 39 negative, 23 boundary, 23 evolution; 14 schema-decided, 72 cross-field, 25 compatibility), the four conformance schemas composing the published v1 schemas, the oracle with its pinned schema layer and 30-code register (15 reused verbatim from `reader-cases.json`), the differential harness with four declared adapter slots, and the coverage, threshold, mutation, defect, contract-gap, and divergence registers. Every corpus gate passes, the oracle agrees with all 111 authored expectations, the mutation score is 22/22, and the harness exits 0 with 444 unmet rows and zero passes because every adapter is unavailable. Design decisions taken during implementation and folded back into the spec: the input is a bundle rather than a bare IR document; the expected diagnostic is the published `common.schema.json` diagnostic with the RFC 6901 pointer beside it; the schema layer collapses a `oneOf` cascade to the deepest failing location, then to a failing ancestor carrying more than one reported descendant; `package.json` is left byte-identical to `main` because issue #9's gate requires it, so the Make targets call `node` directly; and `corpus.json` and `coverage.json` are rendered through the pinned formatter so `make lint` and the byte comparison in FR-039-AC-1 agree. Two cross-field rules were removed as unreachable — the schema closes both the constraint-keyword and edge-category vocabularies before the cross-field layer runs. Task-056 (review, gap analysis, PR) remains.
* **2026-09-04** — Renumbered from Plan-007 to Plan-009: issue #27 merged while this bundle was in review and had taken Plan-007, SR-055.., FND-260.. and TC-320..397. It reserved TC-280..319, Task-048..059, US-008, FR-035..039 and NFR-015..016 for this issue, so this branch keeps those and moves the rest: TC-320..341 to TC-622..419, ERR-051..065 to ERR-100..075, EC-038..048 to EC-063..059, SR-055/056 to SR-075/066, FND-206..398 to FND-715..632. `spec/tests.md` merges both matrices to 419 rows with no gap or duplicate, `spec/spec.md` carries both deliveries, and the six inherited changed-path allow-lists gain this branch's three prefixes.
* **2026-09-04** — Task-056: SR-075 code review FAIL (4 high, 12 medium) and SR-076 gap analysis FAIL (4 high, 10 medium). Every high and every real medium applied; the code review's Dispositions table records each, including the five not acted on and why. The substantive repairs: a real versioning gate over `expectedDigest`; `classify` returns `unknown` for a change no rule models instead of folding to `patch`; nine tautological negative halves rewritten to drive the gate through new `manifest`/`readCase`/`registry`/`thresholds`/`cases` seams; the adapter process branches driven by a real child process; a declared `unsupportedBy` on PROV-002 exercising the accept path; PRES-010 rebuilt as a substring trap that actually discriminates DEF-PROTO-004; a total diagnostic order; a bounded `x-repeat`; a contract-artifact allow-list on provenance; the test-op pin extended to every indexed mutating op; and a changed-path and manifest-diff gate for NFR-016. Gates: 103 vitest and 131 pytest assertions pass, `make conformance` exits 0 with 444 unmet rows and zero passes, mutation score 22/22, `tsc --noEmit` clean.
* **2026-09-04** — Merged `origin/main` at 4e48f08 and re-allocated off the global maximum after issue #19 delivered on PR #50: TC-398..419 to TC-620..641, ERR-061..075 to ERR-100..114, EC-049..059 to EC-063..073, SR-065/066 to SR-075/076, FND-465..632 to FND-715..882, Plan-008 to Plan-009. TC-280..319 and Task-048..056 are kept, being the block issue #27 reserved.
* **2026-09-04** — Repaired this branch's own instance of the defect PR #47 fixed in issue #27. TC-640 asserted `origin/main...HEAD` was non-empty and would have failed forever after this branch merged; TC-641 and TC-643 were negative claims on the same range that would have silently stopped asserting; and `previousManifest()` quietly disabled the whole versioning gate whenever the ref was unreadable, which is what it was doing. The three gates now measure from the merge base with `origin/main`, falling back to the parent of the commit that introduced `conformance/corpus.json` once a squash merge empties that range, and TC-640 gained a tree half. The manifest declares `predecessor`, so a missing comparison is an assertion rather than a skip; the stale declaration fires on movement, not on the merge itself, so `main` stays green the instant this lands. One further assertion forbids any file under `conformance/` from reading an `origin/main..` range at all. Verified in a scratch clone with the branch squash-merged onto `main` and `origin/main` pointed at it: 277 of 279, the two being the poetry-env rows that fail identically there on plain `main`.
* **2026-09-04** — Merged `origin/main` at f412bda, where issue #19 landed. Its matrix ends at TC-621, two rows above the TC-619 its branch carried when this bundle was re-allocated, so this issue's block shifts two more: TC-620..641 to **TC-622..643**. Every other range clears the new global maximum unchanged — ERR-100..114 above ERR-099, EC-063..073 above EC-062, SR-075/076 above SR-074, FND-715..882 above FND-714, Plan-009 above Plan-008, and Task-048..056 below Task-068. The merged matrix is 643 rows, TC-001..643, with no gap and no duplicate, and its execution summary is now generated by `scripts/test-matrix-summary.mjs`, which issue #19 added.
* **2026-09-04** — Two-number verification on `origin/main` at f412bda. Branch: `make test` with no exclusions is **406 of 407 across 10 files**, identical on a second run; `poetry run pytest` is **131 of 131** across 2 files, twice; `make conformance` exits 0 over 111 cases and 4 bases with 444 unmet rows and a 22/22 mutation score; `tsc --noEmit` clean and `biome format .` clean over 398 files. Post-merge rehearsal, built by squash-merging this branch onto f412bda in a scratch clone and repointing `origin/main` at the result so `git diff origin/main...HEAD` returns nothing, this time with `poetry install` run so the shelling-out rows are genuinely exercised: **406 of 407** and **131 of 131**, and TC-232 and TC-279 — which were skipped for want of an environment last time — now run and pass. The single failure is the same in both: issue #19's NFR-021 changed-path gate, filed as #53.
* **2026-09-04** — Probed the landed issue #19 compiler read-only and recorded the first disagreement the oracle has found in a running implementation: GAP-011. The compiler emits `ActorRef` as a `reference` to `ix://agent-ix/core/type/Actor`, which no type declares, no import names and no lock export carries; issue #19's `inspect` accepts the document and this oracle rejects it. `contracts-v1.md` states the resolution rule for relationship targets only, so both readings are defensible. Left unresolved on purpose. Wiring the adapter is filed as #52, which must settle GAP-011 first.

