---
type: log
title: "Plan-009 — Update Log"
description: "Chronological log of changes to the Plan-009 bundle."
---
# Plan-009 — Update Log

## History

* **2026-09-03** — Plan created from the validated issue #20 specification (US-008, FR-035..039, NFR-015, NFR-016, TC-280..319 and TC-620..419, SR-047..054) with nine tasks: schemas and guards, corpus machinery, the oracle on the critical path, two parallel case-authoring tasks, the harness beside them, coverage and import API as the join, determinism evidence, and the review gate.
* **2026-09-03** — Tasks 048–055 done. `conformance/` carries 4 contract-valid input bundles, 111 cases across 22 construct-register families (26 positive, 39 negative, 23 boundary, 23 evolution; 14 schema-decided, 72 cross-field, 25 compatibility), the four conformance schemas composing the published v1 schemas, the oracle with its pinned schema layer and 30-code register (15 reused verbatim from `reader-cases.json`), the differential harness with four declared adapter slots, and the coverage, threshold, mutation, defect, contract-gap, and divergence registers. Every corpus gate passes, the oracle agrees with all 111 authored expectations, the mutation score is 22/22, and the harness exits 0 with 444 unmet rows and zero passes because every adapter is unavailable. Design decisions taken during implementation and folded back into the spec: the input is a bundle rather than a bare IR document; the expected diagnostic is the published `common.schema.json` diagnostic with the RFC 6901 pointer beside it; the schema layer collapses a `oneOf` cascade to the deepest failing location, then to a failing ancestor carrying more than one reported descendant; `package.json` is left byte-identical to `main` because issue #9's gate requires it, so the Make targets call `node` directly; and `corpus.json` and `coverage.json` are rendered through the pinned formatter so `make lint` and the byte comparison in FR-039-AC-1 agree. Two cross-field rules were removed as unreachable — the schema closes both the constraint-keyword and edge-category vocabularies before the cross-field layer runs. Task-056 (review, gap analysis, PR) remains.
* **2026-09-04** — Renumbered from Plan-007 to Plan-009: issue #27 merged while this bundle was in review and had taken Plan-007, SR-055.., FND-260.. and TC-320..397. It reserved TC-280..319, Task-048..059, US-008, FR-035..039 and NFR-015..016 for this issue, so this branch keeps those and moves the rest: TC-320..341 to TC-620..419, ERR-051..065 to ERR-100..075, EC-038..048 to EC-063..059, SR-055/056 to SR-075/066, FND-206..398 to FND-715..632. `spec/tests.md` merges both matrices to 419 rows with no gap or duplicate, `spec/spec.md` carries both deliveries, and the six inherited changed-path allow-lists gain this branch's three prefixes.
* **2026-09-04** — Task-056: SR-075 code review FAIL (4 high, 12 medium) and SR-076 gap analysis FAIL (4 high, 10 medium). Every high and every real medium applied; the code review's Dispositions table records each, including the five not acted on and why. The substantive repairs: a real versioning gate over `expectedDigest`; `classify` returns `unknown` for a change no rule models instead of folding to `patch`; nine tautological negative halves rewritten to drive the gate through new `manifest`/`readCase`/`registry`/`thresholds`/`cases` seams; the adapter process branches driven by a real child process; a declared `unsupportedBy` on PROV-002 exercising the accept path; PRES-010 rebuilt as a substring trap that actually discriminates DEF-PROTO-004; a total diagnostic order; a bounded `x-repeat`; a contract-artifact allow-list on provenance; the test-op pin extended to every indexed mutating op; and a changed-path and manifest-diff gate for NFR-016. Gates: 103 vitest and 131 pytest assertions pass, `make conformance` exits 0 with 444 unmet rows and zero passes, mutation score 22/22, `tsc --noEmit` clean.

