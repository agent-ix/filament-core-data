---
type: log
title: "Plan-008 — Update Log"
description: "Chronological log of changes to the Plan-008 bundle."
---
# Plan-008 — Update Log

## History

* **2026-09-04** — Plan created from the validated issue #19 specification (US-010, FR-045..053, NFR-019..021, TC-398..619, SR-065..072) with twelve tasks on two tracks. Guards first, because six changed-path allowlists on `main` fail on any path a later ticket adds; then the three primitives (injected host, diagnostic registry, JSON loci) that every determinism and safety claim is measured through; then the package side and the frontend on track A, the IR reader and the compatibility work on track B, joined behind `compilePackage` and the CLI. FR-049's registry-completeness criteria are the closing gate rather than a prerequisite. Ids continue after Plan-007's Task-067; Task-048..059 and TC-280..319 remain the parallel issue #20 branch's.
* **2026-09-04** — Tasks 068–078 done. `src/compiler/` now holds the injected host, the diagnostic registry over two namespaces, the exact JSON pointer locator, the RFC 8785 canonical form with its 33 vectors, the lock and the v1 fingerprint, the package resolver, the frontend seam with the registered-and-refused `spec-bundle` dialect, the fifteen-decorator TypeSpec vocabulary and FR-034's identity minting, the lowering to contract IR `1.1.0`, the IR reader and normalizer, the compatibility classifier and the two contract-version projections, the pipeline, and the three commands behind a fifteen-symbol interface. `make test`: **299 vitest cases across 9 files, all passing**; `make lint`, `make build` and `make typecheck` green.

  Six findings the implementation forced into the open, each fixed and recorded rather than worked around: the seam read `common.schema.json` with `node:fs`, which FR-046-CON-4 forbids under `frontend/` (the vocabulary moved to `dialects.mjs`); decorator argument defects were collected but never surfaced; a host refusal is caught by the TypeSpec compiler and re-reported as its own `js-error`, so `UNTRUSTED_MODULE` and `PATH_ESCAPE` arrived as generic compile errors; `SOURCE_OUTSIDE_PACKAGE` was declared and unreachable; three registry codes were named dynamically, defeating the static extractor FR-049-AC-2 requires; and the packed-file gate caught that `fixtures/` is a *published* directory, so the compiler fixture corpus moved to `test/fixtures/compiler/` rather than growing the tarball.

  Two measured limits are recorded rather than papered over. TypeSpec target-checks its own decorators, so the frontend-side constraint-applicability check is unreachable through the nine mapped core decorators; the shared table is what is under test and the rule is exercised through the reader. And the baseline: `origin/main` at 51febd4 is **168 of 173**, not 173/173 — five issue #27 gates assert properties of the promotion's own branch diff and cannot pass once #27 merged. Filed as #48 and repaired here as the state facts they protect.
* **2026-09-04** — Task-079: SR-073 code review and SR-074 gap analysis, both run in parallel subagents against a clean tree, both **FAIL** as recorded. 66 findings: 23 high, 33 medium, 10 low, with heavy overlap between the two. Every high and every medium is fixed; nine of the ten lows are fixed and one records what the reviews found sound. Each review carries a per-finding disposition and a post-remediation verdict.

  The findings worth naming, because they are the ones a green suite was hiding. Six correctness defects the fixture corpus never reached: a constraint or `@unit` on a field typed by a built-in scalar was falsely refused; `keyword in KEYWORD_APPLICABILITY` accepted `Object.prototype` members and crashed the reader that FR-045-CON-4 says can never throw; `isDirectory` swallowed a path-escape refusal so a manifest escaping its package compiled with zero diagnostics; caret ranges admitted prereleases and `alpha.10` sorted below `alpha.2`; the resolver selected against the first constraint it saw, so permuting a manifest's imports changed the resolved version; and an imported package's manifest was outside the fingerprint entirely.

  Five gates that did not gate: `applyDiagnosticLimit` had no call site; `maxDepth` was thrown and caught nowhere, making `LIMIT_MAX_DEPTH` unreachable and the published `fingerprintIr` throw on a deep document; `maxNodes` counted top-level types only and `maxCollectionItems` only fields; the pipeline's `validate` phase was a labelled no-op; and the matrix's `100% mapped` column was a string literal, now measured against the criteria the spec declares and reading `100% mapped (579/579)`.

  Four honesty defects: unknown consumer evidence *downgraded* `breaking` to `unknown`, making a release gate easier to pass with less information; a version uplift carrying any other change reported six phantom entries and hid the real one; the compatibility fixtures used mapping and profile shapes no schema-valid document can carry; and the diff never compared nullability, defaults, presence, roles or variant payloads, so a field could become nullable under a `patch`.

  And the reachability ones: `diffSemanticContract` read its family map from `test/fixtures/`, which `package.json` `files` does not ship, so an installed package would have thrown `ENOENT`; `SOURCE_OUTSIDE_PACKAGE` was declared and unreachable; the declared module `vocabulary.mjs` did not exist. Six test oracles could not fail — the restore rehearsal wrote bytes and read them back, the locale test compared a sort with itself, the path-escape test asserted a disjunction — and each now discriminates.

  Gates after remediation, measured: `make lint`, `make build`, `make typecheck` green; `make test` **299 of 299** across 9 files. `spec/tests.md`: 577 of 579 passed, TC-370 and TC-382 blocked on issue #42.

  Two defects were filed rather than fixed here, both outside this ticket's permitted paths: **#48** (five issue #27 gates assert properties of the promotion's own branch diff and cannot pass once #27 merged — repaired on this branch as the state facts they protect, which is why `origin/main` measures 168/173 and this branch 299/299) and **#49** (`test/semantic-core.test.ts` mutates a tracked file to prove its own `--check` gate, which made three unrelated gates fail at random; mitigated here by one shared `changedPaths` helper that compares content against the merge base).

