{
  "source": "authored",
  "case": "architecture",
  "files": [
    "modules.json",
    "spec/spec.md",
    "spec/model/Count.md",
    "spec/model/Pump.md",
    "spec/model/Sys.md",
    "spec/model/Tank.md",
    "spec/systems/Flow.md",
    "spec/systems/Flow2.md",
    "spec/systems/pipe.md",
    "spec/systems/pump_alloc.md",
    "spec/systems/pump_out.md",
    "spec/systems/sys_pump.md",
    "spec/systems/sys_tank.md",
    "spec/systems/tank_in.md"
  ],
  "derived": [],
  "edits": [],
  "note": "The `architecture` fixture (filament-core-data#173), the bundle QSL intake tests read. It is modelled on QSpec TC-197 fixture Y so its declaration keys line up with the QSpec vectors: value type Count; object types Sys, Pump (with operation run) and Tank as spec-objects-business entities; interfaces Flow (field rate: Count 1..1) and Flow2 (specializes Flow); parts sys_pump and sys_tank owned by Sys; ports pump_out (out) and tank_in (in) typed by Flow; connection pipe pump_out -> tank_in source-to-target; allocation pump_alloc of Pump/run to sys_pump. Every artifact id is its declaration key, word-style with underscores (hyphenated ids await an architect ruling). Lifted under spec-objects-business, edge-vocabulary and spec-objects-architecture at the unmerged SOA #11 head 4215aad (modules.json). Divergences from Y forced by the pinned modules: Count is a value_object with one field `value` (min 0, max 9) because business 0.4.0 has no scalar value type; Sys, Pump and Tank carry a header-only Properties table because entity requires the section. Interfaces are authored with `## Properties` and `## Operations` sections pending the interface feature-table shape of agent-ix/quire-rs#448, plus the `## Contract` yaml fence the pinned manifest requires. Not yet authored: population Q and the navigation relationships Sys/composition and Pump/housing (their end roles and multiplicities need the 0.5.0 `## Relationships` tables). No goldens yet: expected/ waits on IR 2.0.0 (filament-core-data#172) and agent-ix/quire-rs#446."
}
