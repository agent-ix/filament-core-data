{
  "source": "authored",
  "case": "business",
  "files": [
    "spec/spec.md",
    "spec/domain/DM_001-ordering.md",
    "spec/functional/AR_001-order-aggregate.md",
    "spec/functional/EN_001-order-status.md",
    "spec/functional/EV_001-order-placed.md",
    "spec/functional/FR-001-order.md",
    "spec/functional/NE_001-shipment.md",
    "spec/functional/PR_001-fulfilment.md",
    "spec/functional/RP_001-order-repository.md",
    "spec/functional/SM_001-order-lifecycle.md",
    "spec/functional/VO_001-order-line.md",
    "spec/functional/operations.md"
  ],
  "derived": [],
  "edits": [],
  "note": "The `business` fixture (FR-098 inventory), begun by Task-132 with the four artifacts FR-093 lowers: a `domain` with no `## Properties` (fields.state not_applicable, record with fields: []; TC-1335), an `entity` with a collection, an optional field, a Decimal(10,2) [USD] policy and a reference to the enumeration, a `value_object` carrying pattern/min/enumValues constraints, and an `object: enumeration` with a `## Values` table lowered through the module's values locator (TC-1333). Task-133 adds relationships and operations under the vendored spec-objects-business module. Task-133 completes it: one artifact per exported object type (aggregate_root with Members, nested_entity with Parent, repository with Invariants and Operations, event with a json Schema fence, state_machine with States and Transitions tables, process with a Workflow steps table) and operations.md (id OP-001, an entity named Basket) whose operations carry a parameter table, Returns:, and Pre:/Post: references to its own Invariants; frontmatter relationships of contains (domain, entity, aggregate_root, nested_entity), aggregates, composes (value_object), references, owns, emits, carries, raises, consumes, reads, operates_on and persists, plus traces_to/implements/depends_on on the Order entity to targets that exist nowhere (skipped silently, FR-094-AC-6); the Order entity's `capped` ocl fence carries leading whitespace, a tab and two trailing newlines (FR-094-AC-10). Lifted under spec-objects-business and edge-vocabulary (the registry that categorises the verbs). Contract 1.2.0 constructs (FR-143): every artifact satisfies its construct's built-in rules, so the Order Aggregate carries the `single_root` invariant and the Order Lifecycle state machine the `advance` operation. Issue #154 re-authors them for quire-rs FR-075 under module 0.4.0: the mermaid fences become the declared States, Transitions and Workflow tables, DM_001 gains a Ubiquitous Language table, and operation contract lines read Pre:/Post: (quire-rs#454). AR_001 deliberately has no Members table: its `## Members` section is prose, because an aggregate's members derive from its composite relationships (FR-143) and a Members table is refused (agent-ix/spec-objects-business#11, filament-core-data#169)."
}
