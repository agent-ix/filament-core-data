/**
 * SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Generated by the Agent IX TypeScript semantic backend. Do not edit.
 * backend: ix://agent-ix/filament-core-data/backend/typescript@0.1.0
 * contract: 2.0.0
 * package: agent-ix/semantic-kernel@0.2.0
 * fingerprint: sha256:c825c2e71d83f2a68682ef6c9d7b7b3b7bab504aee4d57c38eac71384a5da93b
 */

/**
 * Runtime validators, one per exported type.
 *
 * Each validator is a pure function of its argument. It reads no clock, no
 * environment variable, no file and no socket; it imports nothing outside this
 * package; it never throws, whatever it is handed; and it reports every failure
 * it finds rather than stopping at the first.
 *
 * The narrowing from `unknown` to a generated type happens in exactly one
 * construct — a user-defined type predicate — because this package contains no
 * type assertion. A `prepare` pass builds the value the caller receives, with
 * semantic defaults applied and undeclared members separated; the predicate
 * then decides that value and narrows it.
 */

import {
	VALIDATION_CODES as CODES,
	MAX_VALIDATION_DEPTH,
	copyAccessor,
	fail,
	isPlainObject,
	join,
	ownKeys,
	ownMember,
	sortErrors,
} from "./errors.js";
import type { ValidationError, ValidationResult } from "./errors.js";
import type {
	ClauseLanguage,
	ClauseRef,
	ConstraintDecl,
	ConstraintKeyword,
	DecimalPolicy,
	DecimalPolicyPrecision,
	DecimalPolicyScale,
	DefaultDecl,
	DefaultDeclValue,
	DefaultKind,
	EdgeCategory,
	EnumValue,
	EnumValueDoc,
	EnumValuesConstraint,
	EnumValuesConstraintKeyword,
	EnumValuesConstraintValues,
	EnumValuesConstraintValuesBoolean,
	EnumValuesConstraintValuesNumber,
	EnumValuesConstraintValuesString,
	ExclusiveMaxConstraint,
	ExclusiveMaxConstraintKeyword,
	ExclusiveMaxConstraintValue,
	ExclusiveMaxConstraintValueNumber,
	ExclusiveMaxConstraintValueString,
	ExclusiveMinConstraint,
	ExclusiveMinConstraintKeyword,
	ExclusiveMinConstraintValue,
	ExclusiveMinConstraintValueNumber,
	ExclusiveMinConstraintValueString,
	FieldDecl,
	FieldDeclDoc,
	FieldDeclIdentity,
	FieldDeclNullable,
	FormatConstraint,
	FormatConstraintKeyword,
	FormatConstraintName,
	Identifier,
	KernelScalar,
	MaxConstraint,
	MaxConstraintKeyword,
	MaxConstraintValue,
	MaxConstraintValueNumber,
	MaxConstraintValueString,
	MaxLengthConstraint,
	MaxLengthConstraintKeyword,
	MaxLengthConstraintValue,
	MinConstraint,
	MinConstraintKeyword,
	MinConstraintValue,
	MinConstraintValueNumber,
	MinConstraintValueString,
	MinLengthConstraint,
	MinLengthConstraintKeyword,
	MinLengthConstraintValue,
	Multiplicity,
	MultiplicityLower,
	MultiplicityOrdered,
	MultiplicityUnique,
	MultiplicityUpper,
	NonEmptyConstraint,
	NonEmptyConstraintKeyword,
	OperationDecl,
	PatternConstraint,
	PatternConstraintDialect,
	PatternConstraintKeyword,
	PatternConstraintRegex,
	RelationDecl,
	RelationDeclComposite,
	SemanticId,
	SourceLocus,
	SourceLocusEndColumn,
	SourceLocusEndLine,
	SourceLocusPath,
	SourceLocusStartColumn,
	SourceLocusStartLine,
	TypeRef,
	TypeRefTarget,
	UniqueConstraint,
	UniqueConstraintKeyword,
	UnitSymbol,
} from "./types.js";

function prepareClauseLanguage(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkClauseLanguage(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ClauseLanguage {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ClauseLanguage`. */
export function validateClauseLanguage(input: unknown): ValidationResult<ClauseLanguage> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareClauseLanguage(input, 0);
	if (checkClauseLanguage(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareClauseRef(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "clauseId");
		if (member.state === "value") {
			out["clauseId"] =
				member.value === null
					? member.value
					: prepareIdentifier(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "clauseId");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "language");
		if (member.state === "value") {
			out["language"] =
				member.value === null
					? member.value
					: prepareClauseLanguage(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "language");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceSpan");
		if (member.state === "value") {
			out["sourceSpan"] =
				member.value === null
					? member.value
					: prepareSourceLocus(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceSpan");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["clauseId", "language", "sourceSpan"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkClauseRef(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ClauseRef {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "clauseId");
		const at = join(pointer, "clauseId");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkIdentifier(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "language");
		const at = join(pointer, "language");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkClauseLanguage(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "sourceSpan");
		const at = join(pointer, "sourceSpan");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocus(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["clauseId", "language", "sourceSpan"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ClauseRef`. */
export function validateClauseRef(input: unknown): ValidationResult<ClauseRef> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareClauseRef(input, 0);
	if (checkClauseRef(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareConstraintDecl(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkEnumValuesConstraint(value, "", [], [], depth + 1)) return prepareEnumValuesConstraint(value, depth + 1);
	if (checkExclusiveMaxConstraint(value, "", [], [], depth + 1)) return prepareExclusiveMaxConstraint(value, depth + 1);
	if (checkExclusiveMinConstraint(value, "", [], [], depth + 1)) return prepareExclusiveMinConstraint(value, depth + 1);
	if (checkFormatConstraint(value, "", [], [], depth + 1)) return prepareFormatConstraint(value, depth + 1);
	if (checkMaxConstraint(value, "", [], [], depth + 1)) return prepareMaxConstraint(value, depth + 1);
	if (checkMaxLengthConstraint(value, "", [], [], depth + 1)) return prepareMaxLengthConstraint(value, depth + 1);
	if (checkMinConstraint(value, "", [], [], depth + 1)) return prepareMinConstraint(value, depth + 1);
	if (checkMinLengthConstraint(value, "", [], [], depth + 1)) return prepareMinLengthConstraint(value, depth + 1);
	if (checkNonEmptyConstraint(value, "", [], [], depth + 1)) return prepareNonEmptyConstraint(value, depth + 1);
	if (checkPatternConstraint(value, "", [], [], depth + 1)) return preparePatternConstraint(value, depth + 1);
	if (checkUniqueConstraint(value, "", [], [], depth + 1)) return prepareUniqueConstraint(value, depth + 1);
	return value;
}

function checkConstraintDecl(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ConstraintDecl {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkEnumValuesConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkExclusiveMaxConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkExclusiveMinConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkFormatConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMaxConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMaxLengthConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMinConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMinLengthConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkNonEmptyConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkPatternConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkUniqueConstraint(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ConstraintDecl`. */
export function validateConstraintDecl(input: unknown): ValidationResult<ConstraintDecl> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareConstraintDecl(input, 0);
	if (checkConstraintDecl(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"enumValues",
		"exclusiveMax",
		"exclusiveMin",
		"format",
		"max",
		"maxLength",
		"min",
		"minLength",
		"nonEmpty",
		"pattern",
		"unique",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ConstraintKeyword`. */
export function validateConstraintKeyword(input: unknown): ValidationResult<ConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareConstraintKeyword(input, 0);
	if (checkConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDecimalPolicy(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "precision");
		if (member.state === "value") {
			out["precision"] =
				member.value === null
					? member.value
					: prepareDecimalPolicyPrecision(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "precision");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "scale");
		if (member.state === "value") {
			out["scale"] =
				member.value === null
					? member.value
					: prepareDecimalPolicyScale(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "scale");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["precision", "scale"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkDecimalPolicy(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is DecimalPolicy {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "precision");
		const at = join(pointer, "precision");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDecimalPolicyPrecision(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "scale");
		const at = join(pointer, "scale");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDecimalPolicyScale(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["precision", "scale"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DecimalPolicy`. */
export function validateDecimalPolicy(input: unknown): ValidationResult<DecimalPolicy> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDecimalPolicy(input, 0);
	if (checkDecimalPolicy(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDecimalPolicyPrecision(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkDecimalPolicyPrecision(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is DecimalPolicyPrecision {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/DecimalPolicyPrecision/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 1)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/DecimalPolicyPrecision/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DecimalPolicyPrecision`. */
export function validateDecimalPolicyPrecision(input: unknown): ValidationResult<DecimalPolicyPrecision> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDecimalPolicyPrecision(input, 0);
	if (checkDecimalPolicyPrecision(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDecimalPolicyScale(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkDecimalPolicyScale(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is DecimalPolicyScale {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/DecimalPolicyScale/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 0)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/DecimalPolicyScale/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DecimalPolicyScale`. */
export function validateDecimalPolicyScale(input: unknown): ValidationResult<DecimalPolicyScale> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDecimalPolicyScale(input, 0);
	if (checkDecimalPolicyScale(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDefaultDecl(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "kind");
		if (member.state === "value") {
			out["kind"] =
				member.value === null
					? member.value
					: prepareDefaultKind(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "kind");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareDefaultDeclValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["kind", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkDefaultDecl(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is DefaultDecl {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "kind");
		const at = join(pointer, "kind");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDefaultKind(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDefaultDeclValue(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["kind", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DefaultDecl`. */
export function validateDefaultDecl(input: unknown): ValidationResult<DefaultDecl> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDefaultDecl(input, 0);
	if (checkDefaultDecl(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDefaultDeclValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	const declared: readonly string[] = [];
	const carried: Record<string, unknown> = Object.create(null);
	let carriedAny = false;
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		carried[key] = member.value;
		carriedAny = true;
	}
	if (carriedAny) {
		out["__unknown"] = carried;
	}
	return accessor ? out : { ...out };
}

function checkDefaultDeclValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is DefaultDeclValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DefaultDeclValue`. */
export function validateDefaultDeclValue(input: unknown): ValidationResult<DefaultDeclValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDefaultDeclValue(input, 0);
	if (checkDefaultDeclValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareDefaultKind(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkDefaultKind(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is DefaultKind {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"migration",
		"representation",
		"semantic",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/DefaultKind`. */
export function validateDefaultKind(input: unknown): ValidationResult<DefaultKind> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareDefaultKind(input, 0);
	if (checkDefaultKind(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEdgeCategory(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEdgeCategory(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EdgeCategory {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"behavioral",
		"dataflow",
		"dependency",
		"governance",
		"realization",
		"structural",
		"traceability",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EdgeCategory`. */
export function validateEdgeCategory(input: unknown): ValidationResult<EdgeCategory> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEdgeCategory(input, 0);
	if (checkEdgeCategory(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "doc");
		if (member.state === "value") {
			out["doc"] =
				member.value === null
					? member.value
					: prepareEnumValueDoc(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "doc");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareIdentifier(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["doc", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkEnumValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is EnumValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "doc");
		const at = join(pointer, "doc");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkEnumValueDoc(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkIdentifier(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["doc", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValue`. */
export function validateEnumValue(input: unknown): ValidationResult<EnumValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValue(input, 0);
	if (checkEnumValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValueDoc(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEnumValueDoc(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EnumValueDoc {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValueDoc`. */
export function validateEnumValueDoc(input: unknown): ValidationResult<EnumValueDoc> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValueDoc(input, 0);
	if (checkEnumValueDoc(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareEnumValuesConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "values");
		if (member.state === "value") {
			out["values"] = Array.isArray(member.value)
				? member.value.map((item) => prepareEnumValuesConstraintValues(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "values");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "values"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkEnumValuesConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkEnumValuesConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "values");
		const at = join(pointer, "values");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				if (member.value.length < 1) {
					fail(
						errors,
						at,
						CODES.COLLECTION_TOO_SHORT,
						"the collection is shorter than its declared lower bound",
					);
				}
				for (let index = 0; index < member.value.length; index += 1) {
					checkEnumValuesConstraintValues(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	const declared: readonly string[] = ["keyword", "values"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraint`. */
export function validateEnumValuesConstraint(input: unknown): ValidationResult<EnumValuesConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraint(input, 0);
	if (checkEnumValuesConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEnumValuesConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["enumValues"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraintKeyword`. */
export function validateEnumValuesConstraintKeyword(input: unknown): ValidationResult<EnumValuesConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraintKeyword(input, 0);
	if (checkEnumValuesConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraintValues(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkEnumValuesConstraintValuesBoolean(value, "", [], [], depth + 1)) return prepareEnumValuesConstraintValuesBoolean(value, depth + 1);
	if (checkEnumValuesConstraintValuesNumber(value, "", [], [], depth + 1)) return prepareEnumValuesConstraintValuesNumber(value, depth + 1);
	if (checkEnumValuesConstraintValuesString(value, "", [], [], depth + 1)) return prepareEnumValuesConstraintValuesString(value, depth + 1);
	return value;
}

function checkEnumValuesConstraintValues(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraintValues {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkEnumValuesConstraintValuesBoolean(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkEnumValuesConstraintValuesNumber(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkEnumValuesConstraintValuesString(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraintValues`. */
export function validateEnumValuesConstraintValues(input: unknown): ValidationResult<EnumValuesConstraintValues> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraintValues(input, 0);
	if (checkEnumValuesConstraintValues(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraintValuesBoolean(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEnumValuesConstraintValuesBoolean(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraintValuesBoolean {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraintValuesBoolean`. */
export function validateEnumValuesConstraintValuesBoolean(input: unknown): ValidationResult<EnumValuesConstraintValuesBoolean> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraintValuesBoolean(input, 0);
	if (checkEnumValuesConstraintValuesBoolean(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraintValuesNumber(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEnumValuesConstraintValuesNumber(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraintValuesNumber {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraintValuesNumber`. */
export function validateEnumValuesConstraintValuesNumber(input: unknown): ValidationResult<EnumValuesConstraintValuesNumber> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraintValuesNumber(input, 0);
	if (checkEnumValuesConstraintValuesNumber(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareEnumValuesConstraintValuesString(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkEnumValuesConstraintValuesString(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is EnumValuesConstraintValuesString {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/EnumValuesConstraintValuesString`. */
export function validateEnumValuesConstraintValuesString(input: unknown): ValidationResult<EnumValuesConstraintValuesString> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareEnumValuesConstraintValuesString(input, 0);
	if (checkEnumValuesConstraintValuesString(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMaxConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareExclusiveMaxConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareExclusiveMaxConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkExclusiveMaxConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMaxConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkExclusiveMaxConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkExclusiveMaxConstraintValue(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMaxConstraint`. */
export function validateExclusiveMaxConstraint(input: unknown): ValidationResult<ExclusiveMaxConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMaxConstraint(input, 0);
	if (checkExclusiveMaxConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMaxConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMaxConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMaxConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["exclusiveMax"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMaxConstraintKeyword`. */
export function validateExclusiveMaxConstraintKeyword(input: unknown): ValidationResult<ExclusiveMaxConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMaxConstraintKeyword(input, 0);
	if (checkExclusiveMaxConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMaxConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkExclusiveMaxConstraintValueNumber(value, "", [], [], depth + 1)) return prepareExclusiveMaxConstraintValueNumber(value, depth + 1);
	if (checkExclusiveMaxConstraintValueString(value, "", [], [], depth + 1)) return prepareExclusiveMaxConstraintValueString(value, depth + 1);
	return value;
}

function checkExclusiveMaxConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMaxConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkExclusiveMaxConstraintValueNumber(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkExclusiveMaxConstraintValueString(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMaxConstraintValue`. */
export function validateExclusiveMaxConstraintValue(input: unknown): ValidationResult<ExclusiveMaxConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMaxConstraintValue(input, 0);
	if (checkExclusiveMaxConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMaxConstraintValueNumber(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMaxConstraintValueNumber(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMaxConstraintValueNumber {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMaxConstraintValueNumber`. */
export function validateExclusiveMaxConstraintValueNumber(input: unknown): ValidationResult<ExclusiveMaxConstraintValueNumber> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMaxConstraintValueNumber(input, 0);
	if (checkExclusiveMaxConstraintValueNumber(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMaxConstraintValueString(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMaxConstraintValueString(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMaxConstraintValueString {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMaxConstraintValueString`. */
export function validateExclusiveMaxConstraintValueString(input: unknown): ValidationResult<ExclusiveMaxConstraintValueString> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMaxConstraintValueString(input, 0);
	if (checkExclusiveMaxConstraintValueString(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMinConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareExclusiveMinConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareExclusiveMinConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkExclusiveMinConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMinConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkExclusiveMinConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkExclusiveMinConstraintValue(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMinConstraint`. */
export function validateExclusiveMinConstraint(input: unknown): ValidationResult<ExclusiveMinConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMinConstraint(input, 0);
	if (checkExclusiveMinConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMinConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMinConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMinConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["exclusiveMin"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMinConstraintKeyword`. */
export function validateExclusiveMinConstraintKeyword(input: unknown): ValidationResult<ExclusiveMinConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMinConstraintKeyword(input, 0);
	if (checkExclusiveMinConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMinConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkExclusiveMinConstraintValueNumber(value, "", [], [], depth + 1)) return prepareExclusiveMinConstraintValueNumber(value, depth + 1);
	if (checkExclusiveMinConstraintValueString(value, "", [], [], depth + 1)) return prepareExclusiveMinConstraintValueString(value, depth + 1);
	return value;
}

function checkExclusiveMinConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMinConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkExclusiveMinConstraintValueNumber(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkExclusiveMinConstraintValueString(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMinConstraintValue`. */
export function validateExclusiveMinConstraintValue(input: unknown): ValidationResult<ExclusiveMinConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMinConstraintValue(input, 0);
	if (checkExclusiveMinConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMinConstraintValueNumber(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMinConstraintValueNumber(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMinConstraintValueNumber {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMinConstraintValueNumber`. */
export function validateExclusiveMinConstraintValueNumber(input: unknown): ValidationResult<ExclusiveMinConstraintValueNumber> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMinConstraintValueNumber(input, 0);
	if (checkExclusiveMinConstraintValueNumber(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareExclusiveMinConstraintValueString(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkExclusiveMinConstraintValueString(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ExclusiveMinConstraintValueString {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/ExclusiveMinConstraintValueString`. */
export function validateExclusiveMinConstraintValueString(input: unknown): ValidationResult<ExclusiveMinConstraintValueString> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareExclusiveMinConstraintValueString(input, 0);
	if (checkExclusiveMinConstraintValueString(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFieldDecl(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "constraints");
		if (member.state === "value") {
			out["constraints"] = Array.isArray(member.value)
				? member.value.map((item) => prepareConstraintDecl(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "constraints");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "default");
		if (member.state === "value") {
			out["default"] =
				member.value === null
					? member.value
					: prepareDefaultDecl(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "default");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "doc");
		if (member.state === "value") {
			out["doc"] =
				member.value === null
					? member.value
					: prepareFieldDeclDoc(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "doc");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "identity");
		if (member.state === "value") {
			out["identity"] =
				member.value === null
					? member.value
					: prepareFieldDeclIdentity(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "identity");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "name");
		if (member.state === "value") {
			out["name"] =
				member.value === null
					? member.value
					: prepareIdentifier(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "name");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "nullable");
		if (member.state === "value") {
			out["nullable"] =
				member.value === null
					? member.value
					: prepareFieldDeclNullable(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "nullable");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "type");
		if (member.state === "value") {
			out["type"] =
				member.value === null
					? member.value
					: prepareTypeRef(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "type");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"constraints",
		"default",
		"doc",
		"identity",
		"name",
		"nullable",
		"type",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkFieldDecl(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is FieldDecl {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "constraints");
		const at = join(pointer, "constraints");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkConstraintDecl(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "default");
		const at = join(pointer, "default");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDefaultDecl(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "doc");
		const at = join(pointer, "doc");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkFieldDeclDoc(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "identity");
		const at = join(pointer, "identity");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkFieldDeclIdentity(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "name");
		const at = join(pointer, "name");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkIdentifier(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "nullable");
		const at = join(pointer, "nullable");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkFieldDeclNullable(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "type");
		const at = join(pointer, "type");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkTypeRef(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"constraints",
		"default",
		"doc",
		"identity",
		"name",
		"nullable",
		"type",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FieldDecl`. */
export function validateFieldDecl(input: unknown): ValidationResult<FieldDecl> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFieldDecl(input, 0);
	if (checkFieldDecl(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFieldDeclDoc(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkFieldDeclDoc(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is FieldDeclDoc {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FieldDeclDoc`. */
export function validateFieldDeclDoc(input: unknown): ValidationResult<FieldDeclDoc> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFieldDeclDoc(input, 0);
	if (checkFieldDeclDoc(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFieldDeclIdentity(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkFieldDeclIdentity(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is FieldDeclIdentity {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FieldDeclIdentity`. */
export function validateFieldDeclIdentity(input: unknown): ValidationResult<FieldDeclIdentity> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFieldDeclIdentity(input, 0);
	if (checkFieldDeclIdentity(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFieldDeclNullable(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkFieldDeclNullable(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is FieldDeclNullable {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FieldDeclNullable`. */
export function validateFieldDeclNullable(input: unknown): ValidationResult<FieldDeclNullable> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFieldDeclNullable(input, 0);
	if (checkFieldDeclNullable(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFormatConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareFormatConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "name");
		if (member.state === "value") {
			out["name"] =
				member.value === null
					? member.value
					: prepareFormatConstraintName(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "name");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "name"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkFormatConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is FormatConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkFormatConstraintKeyword(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "name");
		const at = join(pointer, "name");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkFormatConstraintName(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["keyword", "name"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FormatConstraint`. */
export function validateFormatConstraint(input: unknown): ValidationResult<FormatConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFormatConstraint(input, 0);
	if (checkFormatConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFormatConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkFormatConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is FormatConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["format"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FormatConstraintKeyword`. */
export function validateFormatConstraintKeyword(input: unknown): ValidationResult<FormatConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFormatConstraintKeyword(input, 0);
	if (checkFormatConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareFormatConstraintName(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkFormatConstraintName(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is FormatConstraintName {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/FormatConstraintName`. */
export function validateFormatConstraintName(input: unknown): ValidationResult<FormatConstraintName> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareFormatConstraintName(input, 0);
	if (checkFormatConstraintName(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareIdentifier(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkIdentifier(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is Identifier {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/Identifier`. */
export function validateIdentifier(input: unknown): ValidationResult<Identifier> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareIdentifier(input, 0);
	if (checkIdentifier(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareKernelScalar(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkKernelScalar(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is KernelScalar {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"Boolean",
		"Bytes",
		"Decimal",
		"Duration",
		"Integer",
		"JsonObject",
		"String",
		"Timestamp",
		"UUID",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/KernelScalar`. */
export function validateKernelScalar(input: unknown): ValidationResult<KernelScalar> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareKernelScalar(input, 0);
	if (checkKernelScalar(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareMaxConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareMaxConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMaxConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MaxConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMaxConstraintKeyword(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMaxConstraintValue(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxConstraint`. */
export function validateMaxConstraint(input: unknown): ValidationResult<MaxConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxConstraint(input, 0);
	if (checkMaxConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMaxConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MaxConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["max"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxConstraintKeyword`. */
export function validateMaxConstraintKeyword(input: unknown): ValidationResult<MaxConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxConstraintKeyword(input, 0);
	if (checkMaxConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkMaxConstraintValueNumber(value, "", [], [], depth + 1)) return prepareMaxConstraintValueNumber(value, depth + 1);
	if (checkMaxConstraintValueString(value, "", [], [], depth + 1)) return prepareMaxConstraintValueString(value, depth + 1);
	return value;
}

function checkMaxConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MaxConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkMaxConstraintValueNumber(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMaxConstraintValueString(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxConstraintValue`. */
export function validateMaxConstraintValue(input: unknown): ValidationResult<MaxConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxConstraintValue(input, 0);
	if (checkMaxConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxConstraintValueNumber(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMaxConstraintValueNumber(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MaxConstraintValueNumber {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxConstraintValueNumber`. */
export function validateMaxConstraintValueNumber(input: unknown): ValidationResult<MaxConstraintValueNumber> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxConstraintValueNumber(input, 0);
	if (checkMaxConstraintValueNumber(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxConstraintValueString(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMaxConstraintValueString(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MaxConstraintValueString {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxConstraintValueString`. */
export function validateMaxConstraintValueString(input: unknown): ValidationResult<MaxConstraintValueString> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxConstraintValueString(input, 0);
	if (checkMaxConstraintValueString(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxLengthConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareMaxLengthConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareMaxLengthConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMaxLengthConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MaxLengthConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMaxLengthConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMaxLengthConstraintValue(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxLengthConstraint`. */
export function validateMaxLengthConstraint(input: unknown): ValidationResult<MaxLengthConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxLengthConstraint(input, 0);
	if (checkMaxLengthConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxLengthConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMaxLengthConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MaxLengthConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["maxLength"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxLengthConstraintKeyword`. */
export function validateMaxLengthConstraintKeyword(input: unknown): ValidationResult<MaxLengthConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxLengthConstraintKeyword(input, 0);
	if (checkMaxLengthConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMaxLengthConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMaxLengthConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MaxLengthConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/MaxLengthConstraintValue/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 0)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/MaxLengthConstraintValue/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MaxLengthConstraintValue`. */
export function validateMaxLengthConstraintValue(input: unknown): ValidationResult<MaxLengthConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMaxLengthConstraintValue(input, 0);
	if (checkMaxLengthConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareMinConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareMinConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMinConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MinConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMinConstraintKeyword(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMinConstraintValue(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinConstraint`. */
export function validateMinConstraint(input: unknown): ValidationResult<MinConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinConstraint(input, 0);
	if (checkMinConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMinConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MinConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["min"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinConstraintKeyword`. */
export function validateMinConstraintKeyword(input: unknown): ValidationResult<MinConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinConstraintKeyword(input, 0);
	if (checkMinConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkMinConstraintValueNumber(value, "", [], [], depth + 1)) return prepareMinConstraintValueNumber(value, depth + 1);
	if (checkMinConstraintValueString(value, "", [], [], depth + 1)) return prepareMinConstraintValueString(value, depth + 1);
	return value;
}

function checkMinConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MinConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkMinConstraintValueNumber(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkMinConstraintValueString(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinConstraintValue`. */
export function validateMinConstraintValue(input: unknown): ValidationResult<MinConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinConstraintValue(input, 0);
	if (checkMinConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinConstraintValueNumber(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMinConstraintValueNumber(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MinConstraintValueNumber {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinConstraintValueNumber`. */
export function validateMinConstraintValueNumber(input: unknown): ValidationResult<MinConstraintValueNumber> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinConstraintValueNumber(input, 0);
	if (checkMinConstraintValueNumber(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinConstraintValueString(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMinConstraintValueString(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MinConstraintValueString {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinConstraintValueString`. */
export function validateMinConstraintValueString(input: unknown): ValidationResult<MinConstraintValueString> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinConstraintValueString(input, 0);
	if (checkMinConstraintValueString(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinLengthConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareMinLengthConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] =
				member.value === null
					? member.value
					: prepareMinLengthConstraintValue(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMinLengthConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MinLengthConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMinLengthConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMinLengthConstraintValue(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	const declared: readonly string[] = ["keyword", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinLengthConstraint`. */
export function validateMinLengthConstraint(input: unknown): ValidationResult<MinLengthConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinLengthConstraint(input, 0);
	if (checkMinLengthConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinLengthConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMinLengthConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MinLengthConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["minLength"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinLengthConstraintKeyword`. */
export function validateMinLengthConstraintKeyword(input: unknown): ValidationResult<MinLengthConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinLengthConstraintKeyword(input, 0);
	if (checkMinLengthConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMinLengthConstraintValue(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMinLengthConstraintValue(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MinLengthConstraintValue {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/MinLengthConstraintValue/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 0)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/MinLengthConstraintValue/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MinLengthConstraintValue`. */
export function validateMinLengthConstraintValue(input: unknown): ValidationResult<MinLengthConstraintValue> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMinLengthConstraintValue(input, 0);
	if (checkMinLengthConstraintValue(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMultiplicity(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "lower");
		if (member.state === "value") {
			out["lower"] =
				member.value === null
					? member.value
					: prepareMultiplicityLower(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "lower");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "ordered");
		if (member.state === "value") {
			out["ordered"] =
				member.value === null
					? member.value
					: prepareMultiplicityOrdered(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "ordered");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "unique");
		if (member.state === "value") {
			out["unique"] =
				member.value === null
					? member.value
					: prepareMultiplicityUnique(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "unique");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "upper");
		if (member.state === "value") {
			out["upper"] =
				member.value === null
					? member.value
					: prepareMultiplicityUpper(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "upper");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["lower", "ordered", "unique", "upper"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMultiplicity(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is Multiplicity {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "lower");
		const at = join(pointer, "lower");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicityLower(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "ordered");
		const at = join(pointer, "ordered");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicityOrdered(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "unique");
		const at = join(pointer, "unique");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicityUnique(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "upper");
		const at = join(pointer, "upper");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicityUpper(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["lower", "ordered", "unique", "upper"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/Multiplicity`. */
export function validateMultiplicity(input: unknown): ValidationResult<Multiplicity> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMultiplicity(input, 0);
	if (checkMultiplicity(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMultiplicityLower(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMultiplicityLower(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MultiplicityLower {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/MultiplicityLower/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 0)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/MultiplicityLower/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MultiplicityLower`. */
export function validateMultiplicityLower(input: unknown): ValidationResult<MultiplicityLower> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMultiplicityLower(input, 0);
	if (checkMultiplicityLower(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMultiplicityOrdered(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMultiplicityOrdered(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MultiplicityOrdered {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MultiplicityOrdered`. */
export function validateMultiplicityOrdered(input: unknown): ValidationResult<MultiplicityOrdered> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMultiplicityOrdered(input, 0);
	if (checkMultiplicityOrdered(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMultiplicityUnique(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMultiplicityUnique(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MultiplicityUnique {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MultiplicityUnique`. */
export function validateMultiplicityUnique(input: unknown): ValidationResult<MultiplicityUnique> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMultiplicityUnique(input, 0);
	if (checkMultiplicityUnique(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMultiplicityUpper(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkMultiplicityUpper(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is MultiplicityUpper {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/MultiplicityUpper/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 0)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/MultiplicityUpper/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/MultiplicityUpper`. */
export function validateMultiplicityUpper(input: unknown): ValidationResult<MultiplicityUpper> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMultiplicityUpper(input, 0);
	if (checkMultiplicityUpper(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareNonEmptyConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareNonEmptyConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkNonEmptyConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is NonEmptyConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkNonEmptyConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	const declared: readonly string[] = ["keyword"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/NonEmptyConstraint`. */
export function validateNonEmptyConstraint(input: unknown): ValidationResult<NonEmptyConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareNonEmptyConstraint(input, 0);
	if (checkNonEmptyConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareNonEmptyConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkNonEmptyConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is NonEmptyConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["nonEmpty"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/NonEmptyConstraintKeyword`. */
export function validateNonEmptyConstraintKeyword(input: unknown): ValidationResult<NonEmptyConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareNonEmptyConstraintKeyword(input, 0);
	if (checkNonEmptyConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareOperationDecl(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "name");
		if (member.state === "value") {
			out["name"] =
				member.value === null
					? member.value
					: prepareIdentifier(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "name");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "params");
		if (member.state === "value") {
			out["params"] = Array.isArray(member.value)
				? member.value.map((item) => prepareFieldDecl(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "params");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "post");
		if (member.state === "value") {
			out["post"] = Array.isArray(member.value)
				? member.value.map((item) => prepareClauseRef(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "post");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "pre");
		if (member.state === "value") {
			out["pre"] = Array.isArray(member.value)
				? member.value.map((item) => prepareClauseRef(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "pre");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "returns");
		if (member.state === "value") {
			out["returns"] =
				member.value === null
					? member.value
					: prepareTypeRef(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "returns");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"name",
		"params",
		"post",
		"pre",
		"returns",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkOperationDecl(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is OperationDecl {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "name");
		const at = join(pointer, "name");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkIdentifier(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "params");
		const at = join(pointer, "params");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkFieldDecl(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "post");
		const at = join(pointer, "post");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkClauseRef(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "pre");
		const at = join(pointer, "pre");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkClauseRef(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "returns");
		const at = join(pointer, "returns");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkTypeRef(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"name",
		"params",
		"post",
		"pre",
		"returns",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/OperationDecl`. */
export function validateOperationDecl(input: unknown): ValidationResult<OperationDecl> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareOperationDecl(input, 0);
	if (checkOperationDecl(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function preparePatternConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "dialect");
		if (member.state === "value") {
			out["dialect"] =
				member.value === null
					? member.value
					: preparePatternConstraintDialect(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "dialect");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: preparePatternConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "regex");
		if (member.state === "value") {
			out["regex"] =
				member.value === null
					? member.value
					: preparePatternConstraintRegex(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "regex");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["dialect", "keyword", "regex"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkPatternConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is PatternConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "dialect");
		const at = join(pointer, "dialect");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkPatternConstraintDialect(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkPatternConstraintKeyword(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "regex");
		const at = join(pointer, "regex");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkPatternConstraintRegex(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["dialect", "keyword", "regex"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/PatternConstraint`. */
export function validatePatternConstraint(input: unknown): ValidationResult<PatternConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = preparePatternConstraint(input, 0);
	if (checkPatternConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function preparePatternConstraintDialect(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkPatternConstraintDialect(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is PatternConstraintDialect {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["ecma-262"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/PatternConstraintDialect`. */
export function validatePatternConstraintDialect(input: unknown): ValidationResult<PatternConstraintDialect> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = preparePatternConstraintDialect(input, 0);
	if (checkPatternConstraintDialect(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function preparePatternConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkPatternConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is PatternConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["pattern"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/PatternConstraintKeyword`. */
export function validatePatternConstraintKeyword(input: unknown): ValidationResult<PatternConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = preparePatternConstraintKeyword(input, 0);
	if (checkPatternConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function preparePatternConstraintRegex(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkPatternConstraintRegex(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is PatternConstraintRegex {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/PatternConstraintRegex`. */
export function validatePatternConstraintRegex(input: unknown): ValidationResult<PatternConstraintRegex> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = preparePatternConstraintRegex(input, 0);
	if (checkPatternConstraintRegex(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareRelationDecl(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "category");
		if (member.state === "value") {
			out["category"] =
				member.value === null
					? member.value
					: prepareEdgeCategory(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "category");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "composite");
		if (member.state === "value") {
			out["composite"] =
				member.value === null
					? member.value
					: prepareRelationDeclComposite(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "composite");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "multiplicity");
		if (member.state === "value") {
			out["multiplicity"] =
				member.value === null
					? member.value
					: prepareMultiplicity(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "multiplicity");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "target");
		if (member.state === "value") {
			out["target"] =
				member.value === null
					? member.value
					: prepareSemanticId(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "target");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "verb");
		if (member.state === "value") {
			out["verb"] =
				member.value === null
					? member.value
					: prepareIdentifier(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "verb");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"category",
		"composite",
		"multiplicity",
		"target",
		"verb",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkRelationDecl(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is RelationDecl {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "category");
		const at = join(pointer, "category");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkEdgeCategory(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "composite");
		const at = join(pointer, "composite");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkRelationDeclComposite(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "multiplicity");
		const at = join(pointer, "multiplicity");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicity(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "target");
		const at = join(pointer, "target");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSemanticId(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "verb");
		const at = join(pointer, "verb");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkIdentifier(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"category",
		"composite",
		"multiplicity",
		"target",
		"verb",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/RelationDecl`. */
export function validateRelationDecl(input: unknown): ValidationResult<RelationDecl> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareRelationDecl(input, 0);
	if (checkRelationDecl(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareRelationDeclComposite(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkRelationDeclComposite(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is RelationDeclComposite {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "boolean")) {
		fail(errors, pointer, CODES.NOT_A_BOOLEAN, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/RelationDeclComposite`. */
export function validateRelationDeclComposite(input: unknown): ValidationResult<RelationDeclComposite> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareRelationDeclComposite(input, 0);
	if (checkRelationDeclComposite(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSemanticId(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSemanticId(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SemanticId {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SemanticId`. */
export function validateSemanticId(input: unknown): ValidationResult<SemanticId> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSemanticId(input, 0);
	if (checkSemanticId(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocus(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "endColumn");
		if (member.state === "value") {
			out["endColumn"] =
				member.value === null
					? member.value
					: prepareSourceLocusEndColumn(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "endColumn");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "endLine");
		if (member.state === "value") {
			out["endLine"] =
				member.value === null
					? member.value
					: prepareSourceLocusEndLine(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "endLine");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "path");
		if (member.state === "value") {
			out["path"] =
				member.value === null
					? member.value
					: prepareSourceLocusPath(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "path");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceIdentity");
		if (member.state === "value") {
			out["sourceIdentity"] =
				member.value === null
					? member.value
					: prepareSemanticId(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceIdentity");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "startColumn");
		if (member.state === "value") {
			out["startColumn"] =
				member.value === null
					? member.value
					: prepareSourceLocusStartColumn(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "startColumn");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "startLine");
		if (member.state === "value") {
			out["startLine"] =
				member.value === null
					? member.value
					: prepareSourceLocusStartLine(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "startLine");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"endColumn",
		"endLine",
		"path",
		"sourceIdentity",
		"startColumn",
		"startLine",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkSourceLocus(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocus {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "endColumn");
		const at = join(pointer, "endColumn");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocusEndColumn(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "endLine");
		const at = join(pointer, "endLine");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocusEndLine(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "path");
		const at = join(pointer, "path");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocusPath(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "sourceIdentity");
		const at = join(pointer, "sourceIdentity");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSemanticId(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "startColumn");
		const at = join(pointer, "startColumn");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocusStartColumn(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "startLine");
		const at = join(pointer, "startLine");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkSourceLocusStartLine(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"endColumn",
		"endLine",
		"path",
		"sourceIdentity",
		"startColumn",
		"startLine",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocus`. */
export function validateSourceLocus(input: unknown): ValidationResult<SourceLocus> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocus(input, 0);
	if (checkSourceLocus(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocusEndColumn(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSourceLocusEndColumn(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocusEndColumn {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/SourceLocusEndColumn/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 1)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/SourceLocusEndColumn/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocusEndColumn`. */
export function validateSourceLocusEndColumn(input: unknown): ValidationResult<SourceLocusEndColumn> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocusEndColumn(input, 0);
	if (checkSourceLocusEndColumn(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocusEndLine(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSourceLocusEndLine(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocusEndLine {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/SourceLocusEndLine/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 1)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/SourceLocusEndLine/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocusEndLine`. */
export function validateSourceLocusEndLine(input: unknown): ValidationResult<SourceLocusEndLine> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocusEndLine(input, 0);
	if (checkSourceLocusEndLine(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocusPath(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSourceLocusPath(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocusPath {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocusPath`. */
export function validateSourceLocusPath(input: unknown): ValidationResult<SourceLocusPath> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocusPath(input, 0);
	if (checkSourceLocusPath(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocusStartColumn(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSourceLocusStartColumn(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocusStartColumn {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/SourceLocusStartColumn/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 1)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/SourceLocusStartColumn/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocusStartColumn`. */
export function validateSourceLocusStartColumn(input: unknown): ValidationResult<SourceLocusStartColumn> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocusStartColumn(input, 0);
	if (checkSourceLocusStartColumn(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareSourceLocusStartLine(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkSourceLocusStartLine(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is SourceLocusStartLine {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "number")) {
		fail(errors, pointer, CODES.NOT_A_NUMBER, "the value is of the wrong type");
		return false;
	}
	if (Number.isNaN(candidate)) {
		fail(errors, pointer, CODES.NOT_A_NUMBER_VALUE, "the value is NaN");
		return false;
	}
	if (!Number.isFinite(candidate)) {
		fail(errors, pointer, CODES.NOT_FINITE, "the value is not finite");
		return false;
	}
	if (!Number.isInteger(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_INTEGER, "the value is not an integer");
		return false;
	}
	if (!Number.isSafeInteger(candidate)) {
		fail(
			errors,
			pointer,
			CODES.INTEGER_OUT_OF_SAFE_RANGE,
			"the magnitude exceeds Number.MAX_SAFE_INTEGER",
		);
		return false;
	}
	if (!(candidate <= 2147483647)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MAX",
			"the max constraint ix://agent-ix/semantic-core/SourceLocusStartLine/constraint/max is not satisfied",
		);
	}
	if (!(candidate >= 1)) {
		fail(
			errors,
			pointer,
			"agent-ix.semantic-core.MIN",
			"the min constraint ix://agent-ix/semantic-core/SourceLocusStartLine/constraint/min is not satisfied",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/SourceLocusStartLine`. */
export function validateSourceLocusStartLine(input: unknown): ValidationResult<SourceLocusStartLine> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareSourceLocusStartLine(input, 0);
	if (checkSourceLocusStartLine(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareTypeRef(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "decimal");
		if (member.state === "value") {
			out["decimal"] =
				member.value === null
					? member.value
					: prepareDecimalPolicy(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "decimal");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "multiplicity");
		if (member.state === "value") {
			out["multiplicity"] =
				member.value === null
					? member.value
					: prepareMultiplicity(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "multiplicity");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "target");
		if (member.state === "value") {
			out["target"] =
				member.value === null
					? member.value
					: prepareTypeRefTarget(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "target");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "unit");
		if (member.state === "value") {
			out["unit"] =
				member.value === null
					? member.value
					: prepareUnitSymbol(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "unit");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"decimal",
		"multiplicity",
		"target",
		"unit",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkTypeRef(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is TypeRef {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "decimal");
		const at = join(pointer, "decimal");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkDecimalPolicy(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "multiplicity");
		const at = join(pointer, "multiplicity");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMultiplicity(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "target");
		const at = join(pointer, "target");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkTypeRefTarget(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "unit");
		const at = join(pointer, "unit");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkUnitSymbol(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"decimal",
		"multiplicity",
		"target",
		"unit",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/TypeRef`. */
export function validateTypeRef(input: unknown): ValidationResult<TypeRef> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareTypeRef(input, 0);
	if (checkTypeRef(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareTypeRefTarget(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (checkKernelScalar(value, "", [], [], depth + 1)) return prepareKernelScalar(value, depth + 1);
	if (checkSemanticId(value, "", [], [], depth + 1)) return prepareSemanticId(value, depth + 1);
	return value;
}

function checkTypeRefTarget(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is TypeRefTarget {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	const branchErrors: ValidationError[] = [];
	branchErrors.length = 0;
	if (checkKernelScalar(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	branchErrors.length = 0;
	if (checkSemanticId(candidate, pointer, branchErrors, surfaced, depth + 1)) return true;
	fail(
		errors,
		pointer,
		CODES.SHAPE_MISMATCH,
		"the value matches no declared union branch",
	);
	return false;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/TypeRefTarget`. */
export function validateTypeRefTarget(input: unknown): ValidationResult<TypeRefTarget> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareTypeRefTarget(input, 0);
	if (checkTypeRefTarget(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareUniqueConstraint(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "keyword");
		if (member.state === "value") {
			out["keyword"] =
				member.value === null
					? member.value
					: prepareUniqueConstraintKeyword(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "keyword");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["keyword"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkUniqueConstraint(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is UniqueConstraint {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "keyword");
		const at = join(pointer, "keyword");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkUniqueConstraintKeyword(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = ["keyword"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/UniqueConstraint`. */
export function validateUniqueConstraint(input: unknown): ValidationResult<UniqueConstraint> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareUniqueConstraint(input, 0);
	if (checkUniqueConstraint(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareUniqueConstraintKeyword(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkUniqueConstraintKeyword(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is UniqueConstraintKeyword {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["unique"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/UniqueConstraintKeyword`. */
export function validateUniqueConstraintKeyword(input: unknown): ValidationResult<UniqueConstraintKeyword> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareUniqueConstraintKeyword(input, 0);
	if (checkUniqueConstraintKeyword(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareUnitSymbol(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkUnitSymbol(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is UnitSymbol {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!(typeof candidate === "string")) {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/semantic-core/UnitSymbol`. */
export function validateUnitSymbol(input: unknown): ValidationResult<UnitSymbol> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareUnitSymbol(input, 0);
	if (checkUnitSymbol(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}
