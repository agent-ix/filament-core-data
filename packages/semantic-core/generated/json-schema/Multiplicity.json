{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://schemas.agent-ix.org/semantic-core/0.2.0/Multiplicity.json",
	"type": "object",
	"properties": {
		"lower": {
			"type": "integer",
			"minimum": 0,
			"maximum": 2147483647
		},
		"upper": {
			"type": "integer",
			"minimum": 0,
			"maximum": 2147483647
		},
		"ordered": {
			"type": "boolean"
		},
		"unique": {
			"type": "boolean"
		}
	},
	"required": ["lower", "ordered", "unique"],
	"unevaluatedProperties": {
		"not": {}
	},
	"description": "FR-027 multiplicity; absent `upper` is unbounded. Reader: upper >= lower. Every emitted multiplicity carries both `ordered` and `unique`; a producer clamps both to `false` when `upper` is at most one (owner ruling 2026-09-19T15:39:32Z on FCD #199; FLAGS_ON_NON_COLLECTION is deleted, not enforced).",
	"x-agent-ix-semantic-id": "ix://agent-ix/semantic-core/Multiplicity"
}
