---
id: ARCH-RUST-BACKEND
title: "Rust/Serde backend mapping and declared decisions"
status: normative
---
<!-- Generated by scripts/build-rust-backend-docs.mjs. Do not edit by hand:
     `make rust-docs-check` compares this file against the artifacts it is
     rendered from, and `make lint` runs that check. -->
# Rust/Serde backend mapping and declared decisions

Rendered from `src/compiler/backends/rust-serde/mapping-table.json`, which is
the single machine-readable mapping table. 45 rows across
8 axes. A construct that selects no row and no named refusal is an
`agent-ix.rust-backend.UNSUPPORTED_CONSTRUCT`; the mapping is total by
construction, not by claim.

## The mapping

### kind

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `scalar` | `pub struct N(B);` | `transparent` | newtype over the kernel base with try_new and a validating Deserialize | — |
| `record` | `pub struct N { .. }` | — | derived Serialize, member attributes per the field axes, Deserialize routed through try_new | — |
| `enum` | `pub enum N { .. } (fieldless)` | `rename = "<variant name>"` | derived Serialize and Deserialize; the rename is emitted only where the derived identifier differs from the variant name | — |
| `union` | `pub enum N { .. } (unit variant without payloadType, one-field variant with)` | `rename = "<variant name>"` | externally tagged, serde's default; the same conditional rename | — |
| `alias` | `pub struct N(T);` | `transparent` | newtype over the target's Rust type with try_new | — |
| `sequence` | `pub struct N(Vec<I>);` | `transparent` | newtype over the items type with try_new | — |
| `map` | `pub struct N(BTreeMap<String, V>);` | `transparent` | newtype over the values type with try_new; BTreeMap and the fixed String key serialize in one order | — |
| `reference` | `pub struct N(SemanticIdentity);` | `transparent` | newtype over the validated identity, not over the target's Rust type, plus a validating Deserialize | — |

### scalar

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `boolean` | `bool` | — | JSON boolean | — |
| `integer` | `i64` | — | JSON number with no fraction or exponent | — |
| `number` | `f64` | — | JSON number | — |
| `string` | `String` | — | JSON string | — |
| `bytes` | `null` | — | refused: no published artifact states the JSON wire form of bytes (issue #58) | `agent-ix.rust-backend.UNDECLARED_WIRE_FORM` |
| `date` | `Date` | — | generated RFC 3339 full-date newtype over String; JSON string | — |
| `datetime` | `DateTime` | — | generated RFC 3339 date-time newtype over String; JSON string | — |
| `duration` | `Duration` | — | generated ISO 8601 duration newtype over String; JSON string | — |
| `uuid` | `Uuid` | — | generated 8-4-4-4-12 newtype over String; JSON string | — |

### field

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `[object Object]` | `T` | — | the composed member type | — |
| `[object Object]` | `Option<T>` | `default`, `skip_serializing_if = "Option::is_none"` | the composed member type | — |
| `[object Object]` | `Nullable<T>` | — | the composed member type | — |
| `[object Object]` | `Option<Nullable<T>>` | `default`, `skip_serializing_if = "Option::is_none"`, `deserialize_with = "present_or_absent"` | present_or_absent keeps an absent member and a present null distinguishable | — |
| `[object Object]` | `Vec<T>` | — | the composed member type; boundedness does not change it | — |
| `[object Object]` | `Option<Vec<T>>` | `default`, `skip_serializing_if = "Option::is_none"` | the composed member type; boundedness does not change it | — |
| `[object Object]` | `Vec<Nullable<T>>` | — | the composed member type; boundedness does not change it | — |
| `[object Object]` | `Option<Vec<Nullable<T>>>` | `default`, `skip_serializing_if = "Option::is_none"`, `deserialize_with = "present_or_absent"` | present_or_absent keeps an absent member and a present null distinguishable | — |
| `[object Object]` | `null` | — | refused: a member that may never be present has no Rust form serde round-trips | `agent-ix.rust-backend.UNSUPPORTED_MULTIPLICITY` |

### unknownPolicy

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `[object Object]` | `pub struct N { .. }` | `deny_unknown_fields` | serde refuses an unknown member at the boundary | — |
| `[object Object]` | `UnknownMembers` | `flatten` | one flattened BTreeMap<String, SemanticValue> member; validate returns no diagnostic | — |
| `[object Object]` | `UnknownMembers` | `flatten` | one flattened BTreeMap<String, SemanticValue> member; validate returns one UNKNOWN_MEMBER_SURFACED per retained member | `agent-ix.rust-backend.UNKNOWN_MEMBER_SURFACED` |
| `[object Object]` | `null` | — | refused: only a record has a place to put a retained member | `agent-ix.rust-backend.UNKNOWN_POLICY_ON_NON_RECORD` |
| `[object Object]` | `null` | — | no emitted attribute; a non-record kind carries no member set to reject into | — |

### defaultKind

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `none` | `null` | — | no serde default, no metadata default | — |
| `semantic` | `null` | `default = "<generated fn>"` | a serde default drawn from defaultValue, plus the metadata record | `agent-ix.rust-backend.INVALID_DEFAULT_VALUE` |
| `representation` | `null` | — | metadata only: the default belongs to a representation boundary | — |
| `migration` | `null` | — | metadata only: the default belongs to a migration boundary | — |

### extension

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `extensions` | `Vec<Extension>` | — | Extension carries identity, version, required, capability and a SemanticValue payload; an extension is never folded into an unknown member | — |

### metadata

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `roles,origin,relationships,operations,clauses,occurrences` | `generated const metadata` | — | emitted beside the types they belong to, preserving every member the IR carries; an operation is metadata and never a Rust function | — |

### indirection

| Selector | Rust form | Serde | Mechanism | Refusal |
|---|---|---|---|---|
| `field typeRef, single` | `Box<T>` | — | boxed where the target lies in the owner's strongly connected component | — |
| `field typeRef, collection` | `Vec<T>` | — | Vec already carries the indirection, so no Box is introduced | — |
| `alias target` | `Box<T>` | — | boxed where the target lies in the owner's component; a cycle closing through an alias target is broken as surely as one closing through a record field | — |
| `variant payloadType` | `Box<T>` | — | boxed where the payload lies in the owner's component | — |
| `sequence items` | `Vec<I>` | — | Vec already carries the indirection | — |
| `map values` | `BTreeMap<String, V>` | — | BTreeMap already carries the indirection | — |
| `reference target` | `SemanticIdentity` | — | the edge is in the graph and closes cycles there, but the target's Rust type is not materialized at this position, so there is no position to box | — |
| `operation parameter typeRef and returns typeRef` | `generated const metadata` | — | the edge is in the graph and closes cycles there; the mapped type reaches the crate as metadata text, so there is no position to box | — |


## Declared decisions

These are places where the published contract does not decide, and the backend
has to. Each is recorded here as *this backend's reading*, and each is filed
against the artifact's owner so the TypeScript and Python backends reach the
same bytes rather than three separate readings meeting at the issue #7
cross-language gate.

| Decision | What the contract says | What this backend does | Filed as |
|---|---|---|---|
| Union wire form | `contracts-v1.md` calls the construct a discriminated union and the published `rust` target contract's `nativeApi` says "tagged union"; neither names a discriminator member, and the IR carries none | Serde's external tagging, `{"VariantName": payload}`, which is the only form that needs no invented name | [#58](https://github.com/agent-ix/filament-core-data/issues/58) |
| `bytes` wire form | Nothing. `semantic-ir.schema.json` lists `bytes` as a kernel scalar; no `contentEncoding`, no fixture, no corpus case, no `target-verdicts.json` row | Refuses with a blocking `UNDECLARED_WIRE_FORM` and emits no file, rather than pick base64 or an integer array | [#58](https://github.com/agent-ix/filament-core-data/issues/58) |
| `format` names | `agent-ix:plain-text` is the only published format name, and no artifact states its language | Refuses with `UNKNOWN_FORMAT`. Registering a check would be inventing a cross-language rule | [#58](https://github.com/agent-ix/filament-core-data/issues/58) |
| Source licence expression | `target-contract.schema.json` fixes `customSourceLicense` as a `const` of `AGPL-3.0-or-later`; the program's licence clause and `package.json` say `AGPL-3.0-only` | Emits `AGPL-3.0-only`, which is what the ticket mandates, and records the divergence here | [#57](https://github.com/agent-ix/filament-core-data/issues/57) |

## GAP-002 — the locus path pattern under RE2

`common.schema.json#/$defs/sourceLocus` constrains `path` with `^(?!/)(?![A-Za-z]:)(?!.*\\)(?!.*(?:^|/)\.\.(?:/|$))[^\u0000]+$`, four ECMAScript lookaheads.

**Answered, not diverged from.** The published pattern's four ECMAScript
lookaheads compile under no RE2-family engine, so this backend hand-writes the
check. The registry of proved validators carries undefined entry, and an entry is
admitted only through a differential equivalence harness against an ECMA-262
engine reading the pattern out of `schema/semantic/v1/common.schema.json`
rather than a retyped copy.

The corpus budgeted this slot one divergence for exactly this gap —
`conformance/thresholds.json` says "One divergence is permitted for the RE2 locus-pattern gap recorded as GAP-002, which is a contract defect rather than a backend defect." — and the budget goes
**unspent**: `conformance/divergences.json` is byte-unchanged, because the
validator decides the published language exactly rather than approximating it.

The adapter slot's own rationale still reads "No Rust crate exists in this repository. pointerCompatible is false because the published sourceLocus path pattern cannot compile under RE2 (conformance/contract-gaps.json GAP-002), so a Rust reader is expected to locate by its own scheme." and its
`pointerCompatible` is false. Both are stale in the first half now that the
pattern is answered; correcting them is a change to a path this issue may not
write, filed as [#59](https://github.com/agent-ix/filament-core-data/issues/59).

### Two predicates, and the difference between them

`SourceLocusPath::try_new` decides the language the published pattern
*actually* denotes. `SourceLocusPath::is_traversal_free` decides the language
it was evidently *meant* to denote. They differ, and the difference is not
academic: ECMAScript's `.` does not cross a line terminator, so all three of
the pattern's guards — no parent-directory segment, no backslash, no leading
slash — apply only to the value's first line. A path carrying a line feed
followed by `../../etc/passwd` satisfies the published pattern.

The backend exports both, separately named, and never substitutes one for the
other. Where a value governed by this pattern addresses the filesystem — a
compiler request's `outputRoot` is the one such member — the backend requires
both, and refuses with `UNSAFE_OUTPUT_ROOT` otherwise.

The line-terminator finding is not what GAP-002 records, and is filed
separately as [#56](https://github.com/agent-ix/filament-core-data/issues/56).

## GAP-011 — a reference target nothing declares

The issue #19 compiler, compiled read-only over its own `test/fixtures/compiler/packages/assurance` fixture, emits `ix://agent-ix/assurance/type/ActorRef` as a `reference` whose target is `ix://agent-ix/core/type/Actor`. No type in the document declares that identity, the manifest declares no imports, and no lock export names it. Issue #19's own reader accepts the document (`inspect` exits 0 and reports 18 types); this oracle rejects it with one `UNRESOLVED_TYPE_REF` at `/ir/types/0/target`. Both readings are defensible against the contract as written, so two conforming implementations disagree about whether the document is valid — and a generated-package backend must decide whether to emit a type for a reference it cannot resolve.

**Recorded, not decided.** The Rust reader adopts the corpus oracle's reading,
so the disagreement stays visible rather than drifting, and `REF-001..004`
agree by construction. The register names `agent-ix/filament-core-data#9` as the owner, and
that issue is closed, so nobody can currently settle it; the reassignment is
filed as [#59](https://github.com/agent-ix/filament-core-data/issues/59).

The blast radius is confined to the reader. The mapping's `reference` row
emits a newtype over the validated identity whichever way the question settles,
so a resolution moves the reader's rule and the corpus's cases and reaches no
generated type.
